---
phase: 23-coverage-audit
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/app/api/seo/keyword-coverage/route.ts
  - src/components/admin/KeywordCoverageView.tsx
  - src/components/admin/KeywordCoverageLink.tsx
  - src/payload.config.ts
autonomous: false

requirements: [AUDIT-01, AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "An authenticated admin can open a Payload custom view and see, on demand, the list of pages WITHOUT keyword (AUDIT-01)"
    - "The same view shows the list of pages WITH keyword that fail >=1 check, naming the failing checks per row (AUDIT-02)"
    - "Re-running the audit from the view (button/refetch) reflects current DB state — no stale data (AUDIT-03)"
    - "The admin client bundle never imports the analyzer/core directly: data comes from the server endpoint (natural stays server-side)"
    - "View and endpoint are bilingual (es/en) consistent with existing admin UI"
  artifacts:
    - path: "src/app/api/seo/keyword-coverage/route.ts"
      provides: "Authenticated GET endpoint that runs the core server-side and returns the report JSON"
      exports: ["GET"]
    - path: "src/components/admin/KeywordCoverageView.tsx"
      provides: "Client view: fetch endpoint, render the two lists + counts, refresh action"
      contains: "keyword-coverage"
    - path: "src/components/admin/KeywordCoverageLink.tsx"
      provides: "afterNavLinks entry linking to the custom view"
    - path: "src/payload.config.ts"
      provides: "Registration of the view + nav link"
      contains: "keyword-coverage"
  key_links:
    - from: "src/app/api/seo/keyword-coverage/route.ts"
      to: "src/plugins/seo/utils/keywordCoverageAudit.ts"
      via: "import runKeywordCoverageAudit (server-side)"
      pattern: "runKeywordCoverageAudit"
    - from: "src/components/admin/KeywordCoverageView.tsx"
      to: "/api/seo/keyword-coverage"
      via: "fetch in effect/refresh handler"
      pattern: "keyword-coverage"
---

<objective>
Expose the coverage audit inside the Payload admin: a server endpoint that runs the shared core, and a custom admin view that renders the two lists on demand.

Purpose: Give Juan the same audit as the CLI without leaving the admin, while keeping `natural` server-side (the view fetches an endpoint, it never imports the analyzer). Satisfies AUDIT-01/02/03 via the UI path.
Output: `/api/seo/keyword-coverage` GET endpoint, `KeywordCoverageView` + nav link, and Payload config registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-coverage-audit/23-CONTEXT.md

# Shared core built in plan 23-01 — the endpoint wraps this
@src/plugins/seo/utils/keywordCoverageAudit.ts

# Auth + getPayload pattern for the endpoint (admin-only, returns JSON)
@src/app/api/seo/keyword-score/route.ts

# Admin view component + table styling to mirror (fetch in effect, theme vars, bilingual)
@src/components/admin/GSCDashboard.tsx

# Nav link pattern (afterNavLinks)
@src/components/admin/GSCDashboardLink.tsx

<interfaces>
From src/plugins/seo/utils/keywordCoverageAudit.ts (plan 23-01):
```typescript
export async function runKeywordCoverageAudit(
  payload: BasePayload,
  opts?: { locale?: 'es' | 'en' },
): Promise<KeywordCoverageReport>
// KeywordCoverageReport = { generatedAt, counts:{total,noKeyword,failing,passing}, noKeyword: CoverageRow[], failing: CoverageRow[], passing: CoverageRow[] }
// CoverageRow = { collection, id, label, url?, keyword, bucket, score?, failingChecks, amberChecks, naChecks }
```

Payload admin view registration (existing pattern in src/payload.config.ts):
```typescript
admin.components.views.GSCDashboard = {
  Component: '@/components/admin/GSCDashboard#GSCDashboard',
  path: '/gsc-dashboard',
}
admin.components.afterNavLinks = ['@/components/admin/GSCDashboardLink#GSCDashboardLink']
```

Auth pattern (from keyword-score route): `const { user } = await payload.auth({ headers: await headers() })`; return 401 if `!user`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authenticated GET endpoint /api/seo/keyword-coverage</name>
  <files>src/app/api/seo/keyword-coverage/route.ts</files>
  <action>
    Create a GET route mirroring the auth + error handling of src/app/api/seo/keyword-score/route.ts:
    - `getPayload({ config: configPromise })`; authenticate with `payload.auth({ headers: await headers() })`; return 401 `{ error: 'Unauthorized' }` if no user (admin-only — same gate as keyword-score; this prevents leaking the audit publicly).
    - Read optional `?locale=es|en` from the request URL (default 'es').
    - Call `runKeywordCoverageAudit(payload, { locale })` and return `NextResponse.json(report, { status: 200 })`.
    - Wrap in try/catch; on error log server-side and return 500 `{ error: 'Internal server error' }` (do not leak internals).
    - GET (not POST): the audit is a read of current state with no body. Server runs the core so `natural` never reaches the client (key_link).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -c "keyword-coverage/route.ts" | grep -q '^0$' && echo ok</automated>
  </verify>
  <done>GET /api/seo/keyword-coverage returns 401 unauthenticated and the report JSON when authenticated; adds zero tsc errors.</done>
</task>

<task type="auto">
  <name>Task 2: KeywordCoverageView component + nav link</name>
  <files>src/components/admin/KeywordCoverageView.tsx, src/components/admin/KeywordCoverageLink.tsx</files>
  <action>
    KeywordCoverageView ('use client'): mirror GSCDashboard.tsx conventions — `useConfig()` for `serverURL`, `useState` for report/loading/error, fetch `${serverURL}/api/seo/keyword-coverage?locale=<es|en>` in a `useEffect` on mount, plus a "Refrescar / Refresh" button that refetches (AUDIT-03 — on-demand fresh data). Use theme CSS vars (`var(--theme-border-color)`, `var(--theme-bg)`, `var(--theme-text)`) and the same table styling.
    - Header with title (bilingual: "Cobertura de keywords" / "Keyword coverage") + a counts summary (total / sin keyword / fallando / ok) using small metric cards like GSCDashboard's MetricCard.
    - Section 1 "Páginas sin keyword" / "Pages without keyword": table Collection | Página | (edit link to row.url). Source: report.noKeyword. (AUDIT-01)
    - Section 2 "Páginas con checks fallando" / "Pages failing checks": table Collection | Página | Checks que fallan | Avisos(amber). Render row.failingChecks (red) and row.amberChecks; show row.naChecks dimmed or omit. Link the page to row.url. (AUDIT-02)
    - Bilingual: pick language from a simple toggle or default 'es' (Juan's default); keep labels in an es/en map consistent with existing admin UI. IMPORTANT: import ONLY types from the core if needed (`import type { KeywordCoverageReport } from '@/plugins/seo/utils/keywordCoverageAudit'`) — never the runtime function, so `natural` stays out of the client bundle (key_link / threat T-23-SC).
    - Loading and error states like GSCDashboard ("Cargando auditoría..." / error message).
    - KeywordCoverageLink: copy GSCDashboardLink.tsx, link to `${adminPath}/keyword-coverage`, label "Cobertura de keywords" with a 🎯 icon.
  </action>
  <verify>
    <automated>grep -q "import type" src/components/admin/KeywordCoverageView.tsx && ! grep -q "import { runKeywordCoverageAudit" src/components/admin/KeywordCoverageView.tsx && echo "client imports types only"</automated>
  </verify>
  <done>View fetches the endpoint, renders both lists + counts + refresh, is bilingual, and imports only types from the core (no analyzer in the client bundle).</done>
</task>

<task type="auto">
  <name>Task 3: Register view + nav link in Payload config</name>
  <files>src/payload.config.ts</files>
  <action>
    In `admin.components`:
    - Add to `views`: `KeywordCoverage: { Component: '@/components/admin/KeywordCoverageView#KeywordCoverageView', path: '/keyword-coverage' }` (alongside the existing GSCDashboard view).
    - Append `'@/components/admin/KeywordCoverageLink#KeywordCoverageLink'` to `afterNavLinks`.
    Keep formatting consistent with the existing block. Do not touch unrelated config.
  </action>
  <verify>
    <automated>grep -q "keyword-coverage" src/payload.config.ts && grep -q "KeywordCoverageLink" src/payload.config.ts && pnpm exec tsc --noEmit 2>&1 | grep -c "payload.config.ts" | grep -q '^0$' && echo ok</automated>
  </verify>
  <done>The view is reachable at /admin/keyword-coverage and the nav link appears; tsc adds zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the admin coverage view</name>
  <action>Manual verification of the rendered admin view and endpoint after Tasks 1-3 are implemented.</action>
  <what-built>Authenticated /api/seo/keyword-coverage endpoint + a Payload admin view (linked in the nav) that renders the two coverage lists with counts and a refresh button.</what-built>
  <how-to-verify>
    1. Run the dev server and log into the Payload admin.
    2. Click the "Cobertura de keywords" nav link (or visit /admin/keyword-coverage).
    3. Confirm: counts summary renders; "Páginas sin keyword" lists pages with no primaryKeyword; "Páginas con checks fallando" lists keyworded pages with their failing check names; listings (categories/users) do NOT show content checks as failures.
    4. Click Refresh and confirm the data reloads (no stale snapshot).
    5. Spot-check that the lists match `pnpm audit:keywords` output (same shared core).
  </how-to-verify>
  <verify>
    <human-check>Admin view renders both lists with counts; refresh reloads data; listings not penalized on content checks; lists match the CLI output.</human-check>
  </verify>
  <done>Juan confirms the admin view renders correctly and data matches the CLI audit.</done>
  <resume-signal>Type "approved" or describe rendering/data issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser(admin) → API | Authenticated admin requests cross here |
| API → DB | Server runs the audit core with payload access |
| server → client bundle | Analyzer (`natural`) must not cross into the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Information disclosure | /api/seo/keyword-coverage | mitigate | Require `payload.auth` user; return 401 otherwise (same gate as keyword-score) |
| T-23-05 | Elevation of privilege | endpoint | mitigate | Admin-only auth check before running the audit; no query params alter scope beyond locale |
| T-23-SC | Tampering | client bundle | mitigate | View imports `import type` only from the core; runtime analyzer + `natural` stay server-side via the endpoint |
| T-23-06 | Denial of service | endpoint | accept | Bounded by core's `limit: 1000`/depth 1; admin-only, low request volume |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` adds no NEW errors (route, view, link, config contribute zero).
- `pnpm exec vitest run` stays green (no regressions).
- Endpoint returns 401 unauthenticated, report JSON when authenticated.
- Human-verify: admin view renders both lists, refresh works, listings not penalized on content checks.
</verification>

<success_criteria>
- AUDIT-01/02/03 satisfied via the admin UI path (lists + on-demand refresh).
- natural stays server-side (client imports types only).
- View and endpoint bilingual and consistent with existing admin styling.
</success_criteria>

<output>
Create `.planning/phases/23-coverage-audit/23-02-SUMMARY.md` when done.
</output>
