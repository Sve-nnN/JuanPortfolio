---
phase: 24-keyword-research-population
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Posts/index.ts
  - src/collections/Pages/index.ts
  - src/collections/Categories.ts
  - src/collections/Users/index.ts
  - src/payload-types.ts
  - src/components/admin/KeywordScorePanel.tsx
  - src/app/api/seo/keyword-score/route.ts
  - src/plugins/seo/utils/keywordCoverageAudit.ts
  - src/plugins/seo/utils/keywordCoverageAudit.test.ts
autonomous: true
requirements: [RESEARCH-01, RESEARCH-02]
must_haves:
  truths:
    - "primaryKeyword acepta una keyword distinta por locale (es/en) en Posts, Pages, Categories y Users"
    - "payload-types.ts refleja el campo localizado tras regenerar tipos"
    - "El panel de Fase 22 sigue mostrando métricas y semáforo leyendo la keyword del locale activo (no se rompió)"
    - "La auditoría de Fase 23 reporta cobertura por locale: un doc con keyword en es pero no en en aparece como gap en en"
    - "vitest sigue verde; tsc no introduce errores nuevos en archivos tocados"
  artifacts:
    - path: "src/collections/Posts/index.ts"
      provides: "primaryKeyword localized:true"
      contains: "localized: true"
    - path: "src/collections/Pages/index.ts"
      provides: "primaryKeyword localized:true"
      contains: "localized: true"
    - path: "src/plugins/seo/utils/keywordCoverageAudit.test.ts"
      provides: "test de divergencia de cobertura por locale"
      contains: "locale"
  key_links:
    - from: "src/components/admin/KeywordScorePanel.tsx"
      to: "fields['primaryKeyword'].value"
      via: "form state del locale activo"
      pattern: "primaryKeyword"
    - from: "src/plugins/seo/utils/keywordCoverageAudit.ts"
      to: "payload.find({ locale })"
      via: "query por locale resuelve la relación localizada"
      pattern: "locale"
---

<objective>
Convertir `primaryKeyword` en un campo localizado (keyword es/en distinta por documento) en Posts, Pages, Categories y Users, regenerar los tipos de Payload, y garantizar que las features ya shipped de las Fases 22 (panel/semáforo) y 23 (auditoría de cobertura) sigan funcionando y se vuelvan locale-aware.

Esta es la base riesgosa del milestone: toca el modelo de datos que consumen tres fases ya completas. El plan 24-02 (población) depende de que esto quede sólido.

Purpose: Fidelidad al research bilingüe (cada locale puede targetear su propia keyword) sin romper lo construido.
Output: Campo localizado + tipos regenerados + Fase 22 verificada + Fase 23 locale-aware + tests actualizados en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-keyword-research-population/24-CONTEXT.md

<notes>
- DB = MongoDB (`mongooseAdapter`, src/payload.config.ts:114). Mongo es schemaless: localizar un campo existente NO requiere archivos de migración. Payload pasa a almacenar el valor como subdocumento `{ es, en }`. Los valores `primaryKeyword` planos ya existentes (los ~26 docs con keyword del baseline) quedarán fuera del slot de locale; NO intentes migrarlos a mano: el plan 24-02 los re-puebla desde content/keywords_map.json (fuente de verdad más completa). Documentá esto en el SUMMARY como comportamiento esperado.
- `locales` configurados: `es` (default), `en`. `fallback: true`.
- keyword-metrics NO se localiza (queda fuera de scope, decisión CONTEXT). Solo la RELACIÓN desde Posts/Pages/Categories/Users se localiza.
- `natural` (NLP) debe quedar SOLO server-side: nunca importes keywordCoverageAudit.ts ni seoAnalyzer.ts en componentes client. El panel y la vista llegan a la lógica vía endpoints. No cambies ese patrón.
</notes>

<interfaces>
primaryKeyword hoy (idéntico en Posts/Pages/Categories/Users), a localizar:
```ts
{
  name: 'primaryKeyword',
  type: 'relationship',
  relationTo: 'keyword-metrics',
  admin: { position: 'sidebar' },
}
```
Posts: src/collections/Posts/index.ts:152 — Pages: src/collections/Pages/index.ts:211 — Categories: src/collections/Categories.ts:45 — Users: src/collections/Users/index.ts:251.

keywordCoverageAudit (Fase 23) YA es locale-aware en plumbing:
```ts
export function auditDoc(args: AuditDocArgs): CoverageRow  // args incluye locale: 'es'|'en'
export async function runKeywordCoverageAudit(
  payload, opts?: { locale?: 'es'|'en' }
): Promise<KeywordCoverageReport>  // hace payload.find({ ..., depth:1, locale })
```
KeywordCoverageView (src/components/admin/KeywordCoverageView.tsx) ya tiene toggle `lang` es/en → fetch `?locale=${lang}` → endpoint /api/seo/keyword-coverage → runKeywordCoverageAudit({locale}). Endpoint en src/app/api/seo/keyword-coverage/route.ts.

KeywordScorePanel (Fase 22) lee:
```ts
const pkRaw = fields?.['primaryKeyword']?.value ?? initialData?.primaryKeyword
// Payload entrega en form state el valor del LOCALE ACTIVO para campos localizados.
// useLocale() ya provee `code`; el POST a /api/seo/keyword-score ya envía `locale`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localizar primaryKeyword en las 4 colecciones y regenerar tipos</name>
  <files>src/collections/Posts/index.ts, src/collections/Pages/index.ts, src/collections/Categories.ts, src/collections/Users/index.ts, src/payload-types.ts</files>
  <action>
  Agregá `localized: true` al campo `primaryKeyword` (type relationship a keyword-metrics) en las cuatro colecciones: Posts (línea ~152), Pages (~211), Categories (~45), Users (~251). Localizá las cuatro por consistencia del panel locale-aware (recomendación CONTEXT). No toques `semanticKeywords` salvo que ya esté localizado de forma coherente; solo `primaryKeyword`. No agregues campos nuevos a keyword-metrics (fuera de scope).
  Tras el cambio, regenerá los tipos: `pnpm generate:types`. Esto reescribe src/payload-types.ts; `primaryKeyword` no cambia de tipo TS (sigue siendo relación) pero confirmá que el archivo se regeneró sin error.
  Si el build del admin lo requiere por cambio de importmap, corré `pnpm generate:importmap` (no debería ser necesario: no se agregan componentes).
  NO intentes migrar a mano los valores planos existentes en Mongo: el plan 24-02 los re-puebla desde keywords_map.json. Dejá nota en el SUMMARY.
  </action>
  <verify>
    <automated>grep -c "localized: true" src/collections/Posts/index.ts src/collections/Pages/index.ts src/collections/Categories.ts src/collections/Users/index.ts && pnpm generate:types && pnpm exec tsc --noEmit 2>&1 | grep -E "Posts/index|Pages/index|Categories|Users/index|payload-types" | grep -v "^#" | wc -l</automated>
  </verify>
  <done>Las 4 colecciones tienen `localized: true` en primaryKeyword; `pnpm generate:types` corre sin error; tsc no reporta errores nuevos en los archivos tocados (conteo 0 sobre el baseline de esos archivos).</done>
</task>

<task type="auto">
  <name>Task 2: Verificar/ajustar el panel y endpoint de Fase 22 con el campo localizado</name>
  <files>src/components/admin/KeywordScorePanel.tsx, src/app/api/seo/keyword-score/route.ts</files>
  <action>
  Verificá que KeywordScorePanel sigue leyendo la keyword del LOCALE ACTIVO. Payload entrega en `fields['primaryKeyword'].value` el valor del locale activo para campos localizados, y el panel ya usa `useLocale()` para `locale` y lo envía en el POST a /api/seo/keyword-score. En la mayoría de los casos NO se necesita cambio de código.
  Revisá el fallback `initialData?.primaryKeyword`: con campo localizado, `initialData` puede traer la forma del locale activo. Si detectás que tras localizar el panel deja de resolver `keywordId`/`keywordString` al cambiar de locale en el admin, ajustá la lectura para que respete el locale activo (sin importar `natural` ni lógica server en el bundle client). Mantené las funciones puras y el patrón de fetch existente.
  Confirmá que /api/seo/keyword-score sigue respetando `locale` del body (ya lo hace, línea 51). El fetch de métricas usa `/api/keyword-metrics/${keywordId}` y keyword-metrics NO está localizado, así que ese fetch no requiere locale.
  </action>
  <verify>
    <automated>grep -q "useLocale" src/components/admin/KeywordScorePanel.tsx && grep -q "locale" src/app/api/seo/keyword-score/route.ts && pnpm exec tsc --noEmit 2>&1 | grep -E "KeywordScorePanel|keyword-score" | grep -v "^#" | wc -l</automated>
  </verify>
  <done>El panel lee la keyword del locale activo y envía `locale` al endpoint; el endpoint respeta `locale`; tsc no introduce errores nuevos en estos dos archivos (conteo 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hacer locale-aware la auditoría de Fase 23 y actualizar tests</name>
  <files>src/plugins/seo/utils/keywordCoverageAudit.ts, src/plugins/seo/utils/keywordCoverageAudit.test.ts</files>
  <behavior>
    - Un doc con keyword resuelta en locale 'es' pero null en 'en': auditado con locale 'en' → bucket 'noKeyword'; auditado con locale 'es' → bucket 'passing' o 'failing' (no 'noKeyword'). Demuestra cobertura por locale.
    - `runKeywordCoverageAudit(payload, {locale:'en'})` consulta `payload.find({..., locale:'en'})` y por tanto resuelve la relación localizada del locale en; los gaps reportados son específicos del locale solicitado.
    - Los tests existentes (1-7) siguen pasando sin cambios de semántica.
  </behavior>
  <action>
  La plumbing ya es locale-aware: `runKeywordCoverageAudit` ya recibe `{locale}` y hace `payload.find({..., depth:1, locale})`, y `auditDoc` ya toma `locale`. Con primaryKeyword localizado, consultar por locale ahora resuelve la keyword específica de ese locale (antes devolvía la misma para ambos). Verificá que `extractKeyword` y el bucketing siguen correctos con la relación localizada resuelta a profundidad 1.
  Si encontrás algún punto donde el locale no se propaga (revisá las 4 llamadas a payload.find y los 4 bloques de mapeo posts/pages/categories/users), corregilo para que cada doc se evalúe contra la keyword del locale consultado.
  Agregá al test un caso de DIVERGENCIA por locale usando `auditDoc` (unidad pura, sin DB): mismo doc, dos pasadas — una con `keyword: null, locale:'en'` que debe bucketear 'noKeyword', y otra con `keyword:'<term>', locale:'es'` con title/meta/slug/content óptimos que debe bucketear 'passing'. Esto documenta que un doc con keyword en es y no en en es un gap en en.
  No introduzcas import de `natural` en client; este módulo es server-only (ya documentado en su header).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/plugins/seo/utils/keywordCoverageAudit.test.ts 2>&1 | tail -5 && pnpm exec tsc --noEmit 2>&1 | grep -E "keywordCoverageAudit" | grep -v "^#" | wc -l</automated>
  </verify>
  <done>El test de divergencia por locale pasa; los 7 tests existentes siguen verdes; tsc no introduce errores nuevos en keywordCoverageAudit.ts/.test.ts (conteo 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → /api/seo/* | El panel/vista (client) cruzan a lógica server-only que usa `natural` |
| DB schema change | Localizar un campo existente reestructura el almacenamiento de la relación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information disclosure | `natural` en bundle client | mitigate | Mantener keywordCoverageAudit.ts/seoAnalyzer.ts server-only; client solo importa types y consume endpoints (patrón ya vigente, no romperlo) |
| T-24-02 | Tampering | datos primaryKeyword existentes al localizar | accept | Mongo schemaless; valores planos previos quedan fuera del slot de locale y se re-pueblan en 24-02 desde keywords_map.json (fuente de verdad). Sin pérdida neta |
| T-24-03 | Denial of service | auditoría por locale (2 pasadas posibles en UI) | accept | Cap por colección ya existe (COLLECTION_LIMIT=1000, T-23-03); el toggle hace una sola pasada por request |
</threat_model>

<verification>
- `pnpm generate:types` corre sin error.
- `pnpm exec tsc --noEmit` no agrega errores nuevos sobre el baseline (114 pre-existentes en tests/scripts) en los archivos tocados.
- `pnpm exec vitest run` permanece verde (775+).
- Las 4 colecciones tienen `localized: true` en primaryKeyword.
- Test de divergencia por locale presente y verde.
</verification>

<success_criteria>
- primaryKeyword localizado en Posts, Pages, Categories, Users.
- Tipos regenerados.
- Fase 22 (panel + endpoint) verificada/ajustada para locale activo, sin regresión.
- Fase 23 (auditoría) confirmada locale-aware con test de divergencia.
- tsc sin errores nuevos; vitest verde.
</success_criteria>

<output>
Create `.planning/phases/24-keyword-research-population/24-01-SUMMARY.md` when done.
</output>
