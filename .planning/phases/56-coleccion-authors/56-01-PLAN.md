---
phase: 56-coleccion-authors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Authors/index.ts
  - src/payload.config.ts
  - src/collections/Posts/index.ts
  - src/collections/Posts/hooks/populateAuthors.ts
  - src/utilities/resolvePostAuthors.ts
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
  - src/app/(frontend)/[locale]/author/[slug]/page.tsx
  - src/app/(frontend)/[locale]/authors/page.tsx
  - src/app/(frontend)/(sitemaps)/authors-sitemap.xml/route.ts
  - src/app/(frontend)/sitemap/page.tsx
  - src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx
  - src/components/AuthorCard/index.tsx
  - src/utilities/generateSchema.ts
  - src/components/admin/LiveUrlLink.tsx
  - src/scripts/migrate-authors.ts
  - src/scripts/verify-authors-collection.ts
  - src/payload-types.ts
  - package.json
autonomous: true
requirements: [AUTHORS-01, AUTHORS-02, AUTHORS-03]

must_haves:
  truths:
    - "La colección Authors aparece en el admin bajo el grupo Contenido, editable con name/slug/bio/avatar/jobTitle/socialMedia/expertise/education/experience + tab SEO"
    - "Un Post puede relacionarse con uno o más Authors vía el campo postAuthors, y la asociación persiste al guardar (sin tocar el campo authors→users existente)"
    - "La author page pública muestra la misma información que antes: prefiere Authors (por slug), cae a users si no hay match — un solo deploy es seguro sin importar cuándo corra la migración"
    - "El authors-sitemap genera las mismas URLs /authors/{slug} y /en/authors/{slug}: prefiere Authors, cae a users si la colección está vacía"
    - "AuthorCard, PostHero, JSON-LD renderizan idéntico desde postAuthors o desde el fallback authors→users vía el helper resolvePostAuthors"
    - "pnpm exec tsc --noEmit no introduce nuevos errores en src/; el diff de generate:types se limita a la colección Authors + el campo postAuthors"
  artifacts:
    - path: "src/collections/Authors/index.ts"
      provides: "Colección Authors pública (read: () => true) con paridad de campos author-facing de Users"
      contains: "slug: 'authors'"
    - path: "src/utilities/resolvePostAuthors.ts"
      provides: "Helper con forma normalizada + fallback postAuthors→authors(users)"
      exports: ["resolvePostAuthors", "NormalizedAuthor"]
    - path: "src/scripts/migrate-authors.ts"
      provides: "Migración idempotente Local API (RUN diferido a Juan)"
    - path: "src/scripts/verify-authors-collection.ts"
      provides: "Verificación de paridad post-migración (RUN diferido a Juan)"
  key_links:
    - from: "src/collections/Posts/index.ts"
      to: "collection 'authors'"
      via: "campo postAuthors relationTo authors hasMany"
      pattern: "relationTo:\\s*'authors'"
    - from: "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"
      to: "collection 'authors' con fallback users"
      via: "payload.find authors by slug || users by slug"
      pattern: "collection:\\s*'authors'"
    - from: "src/app/(frontend)/(sitemaps)/authors-sitemap.xml/route.ts"
      to: "collection 'authors' con fallback users"
      via: "find authors where slug exists || users"
      pattern: "collection:\\s*'authors'"
---

<objective>
Introducir una colección `Authors` real (separada de la colección de auth `users`), relacionarla con `Posts` mediante un campo NUEVO y aditivo `postAuthors`, y hacer que las ~11 superficies que hoy leen autores prefieran `Authors` con FALLBACK a `users`. Incluye la migración de datos idempotente y el script de verificación (ambos con RUN diferido a Juan).

Purpose: Cierra AUTHORS-01/02/03. Desacopla el contenido de autor del sistema de auth, elimina la razón del workaround `populatedAuthors`, y prepara la limpieza de Phase 58 — todo con paridad exacta de datos y URLs.

Output: Colección `Authors` + campo `postAuthors` + helper `resolvePostAuthors` + cutover de lecturas con fallback + scripts `migrate:authors` / `verify:authors` + types regenerados.

**Seguridad de deploy (crítico):** La estrategia es ADITIVA con FALLBACK. El campo viejo `authors→users` NO se toca y permanece como fuente viva. Como cada lectura prefiere `Authors` pero cae a `users` cuando no hay match, **UN SOLO deploy de este plan es seguro sin importar si la migración ya corrió o no**: sin migración, todo resuelve por fallback exactamente como hoy; con migración, resuelve desde Authors. El hazard de ordenamiento del RESEARCH (§F) queda neutralizado por el fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-coleccion-authors/56-CONTEXT.md
@.planning/phases/56-coleccion-authors/56-RESEARCH.md
@.planning/ROADMAP.md

@src/collections/Users/index.ts
@src/collections/Posts/hooks/populateAuthors.ts
@src/fields/slug.ts
@src/scripts/migrate-blog-listing-to-page.ts

<interfaces>
<!-- Contratos ya verificados en el codebase. El ejecutor usa estos directamente — sin exploración. -->

Grupo admin (src/utilities/adminGroups.ts):
```ts
export const ADMIN_GROUP = {
  CONTENIDO: { es: 'Contenido', en: 'Content' },
  SITIO: { es: 'Sitio', en: 'Site' },
  // ...
} as const
```

slugField (src/fields/slug.ts) — respeta un `value` string explícito (lo lowercasea y reemplaza espacios por `-`); si no, deriva de `fieldToUse`. Uso: `slugField('name')` para Authors.

seoFields (src/utilities/seo/seoFields.ts):
```ts
export const seoFields = ({ defaults = {}, jsonLd = true }: SEOFieldsConfig = {}): Field[]
```
Users lo usa como un tab: `{ label: 'SEO', fields: [...seoFields()] }`. Produce el grupo `meta` (title/description/keywords), canonical, noindex/nofollow, og, twitter, seoScore, seoAnalysis, schema.

Campos author-facing EXACTOS a replicar de Users (tab "Perfil"), EXCLUYENDO todo lo de auth:
- `name` text required
- `role` text (en Users tiene access adminsField — en Authors va como texto simple SIN access controlado)
- `jobTitle` text localized
- `bio` textarea localized, label { en: 'Bio', es: 'Biografía' }
- `expertise` array [ { topic: text required } ]
- `socialMedia` group [ linkedin, github, twitter, website (todos text) ]
- `education` array [ degree(req), institution, logo(upload→media), startDate(date monthOnly), endDate(date monthOnly), certificate(upload→media), description(textarea) ]
- `experience` array [ company(text), role(text), startDate(date), endDate(date), description(textarea) ]
- `avatar` upload relationTo media
- `slugField('name')`  (unique, index — mismo patrón que Users)
- tab SEO: `...seoFields()`
EXCLUIR: auth (email/password), `credentials` (deprecado), `liveUrl` (ui), `primaryKeyword` (relación opcional), y el `ensureUniqueSlug` beforeChange atado a `collection: 'users'`.

Posts.authors + populatedAuthors (src/collections/Posts/index.ts ~L260):
```ts
{ name: 'authors', type: 'relationship', relationTo: 'users', hasMany: true, admin: { position: 'sidebar' } },
{ name: 'populatedAuthors', type: 'array', access: { update: () => false },
  admin: { disabled: true, readOnly: true }, fields: [ {name:'id',type:'text'}, {name:'name',type:'text'} ] },
```
Hook `populateAuthors` (afterRead) hoy hace `findByID({ collection: 'users', depth: 0 })` por cada `doc.authors` y emite `{ id, name, slug }`.

AuthorCard (src/components/AuthorCard/index.tsx) espera:
```ts
author: Pick<User, 'name' | 'jobTitle' | 'bio' | 'avatar' | 'slug' | 'socialMedia'>
```
Se invoca en blog post page con `post.authors[0]` (objeto User resuelto a depth 2).

generateSchema (src/utilities/generateSchema.ts ~L58) para posts:
```ts
const authors = doc.populatedAuthors || doc.authors  // usa authors[0].name
```

Sitemap authors (route.ts): `find users where slug exists` → `/authors/{slug}` + `/en/authors/{slug}`, cacheado `unstable_cache(['authors-sitemap'], { tags: ['authors-sitemap'] })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Colección Authors + campo postAuthors en Posts + generate:types</name>
  <files>src/collections/Authors/index.ts, src/payload.config.ts, src/collections/Posts/index.ts, src/payload-types.ts</files>
  <action>
Cierra AUTHORS-01 y AUTHORS-02.

(1) Crear `src/collections/Authors/index.ts` exportando `export const Authors: CollectionConfig`. `slug: 'authors'`. `access: { read: () => true }` (público — no hay datos de auth; elimina la razón del workaround populatedAuthors, ver RESEARCH §D/Security). `admin: { group: ADMIN_GROUP.CONTENIDO, useAsTitle: 'name', defaultColumns: ['name', 'slug'] }`. `timestamps: true`. NO poner `auth: true`. Estructurar los campos en el MISMO patrón de tabs que Users: tab "Perfil" con los campos author-facing enumerados en el bloque `<interfaces>` (name, role como texto simple SIN access, jobTitle localized, bio localized, expertise, socialMedia, education, experience, avatar, `slugField('name')`), y tab "SEO" con `...seoFields()`. Importar `slugField` de `@/fields/slug`, `seoFields` de `@/utilities/seo/seoFields`, `ADMIN_GROUP` de `@/utilities/adminGroups`. Copiar los sub-campos de education/experience/socialMedia/expertise VERBATIM de Users (mismos names/types/labels/admin.date) para garantizar paridad de datos con la migración. NO incluir `credentials`, `liveUrl`, `primaryKeyword`, ni el hook `ensureUniqueSlug` (ese consulta `collection: 'users'`; el `unique` de slugField ya cubre unicidad).

(2) Registrar en `src/payload.config.ts`: agregar `import { Authors } from './collections/Authors'` junto a los demás imports de colecciones, y añadir `Authors` al array `collections` (después de `Users`, ~L127). Opcional pero recomendado por paridad admin: añadir una entrada en el bloque `admin.components ... collections` si ahí se describen las demás (imitar la entrada de `Users`); si no aporta, omitir.

(3) En `src/collections/Posts/index.ts`, inmediatamente DESPUÉS del campo `authors` (~L261) y ANTES de `populatedAuthors`, agregar el campo NUEVO `postAuthors`: `{ name: 'postAuthors', type: 'relationship', relationTo: 'authors', hasMany: true, admin: { position: 'sidebar', description: 'Autores desde la colección Authors (preferido). El campo authors→users queda como fallback hasta Phase 58.' } }`. NO modificar el campo `authors` existente (sigue `relationTo: 'users'`). NO tocar `populatedAuthors` en esta task (se actualiza su hook en Task 3).

(4) Correr `pnpm generate:types`. El diff esperado y ACOTADO: nueva interface `Author` + entrada `authors` en `Config['collections']`, y el campo `postAuthors` en la interface `Post`. Cualquier otro cambio fuera de eso es una señal de error — revisar antes de continuar.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "src/collections/(Authors|Posts)|src/payload" || echo "no new errors in changed files"; git diff --stat src/payload-types.ts</automated>
  </verify>
  <done>`Authors` compila y está registrada en payload.config; `postAuthors` existe en Posts; generate:types regeneró payload-types.ts con SOLO Author + postAuthors nuevos; tsc sin errores nuevos en los archivos tocados. (AUTHORS-01, AUTHORS-02)</done>
</task>

<task type="auto">
  <name>Task 2: Helper resolvePostAuthors con forma normalizada + fallback</name>
  <files>src/utilities/resolvePostAuthors.ts</files>
  <action>
Crear el helper reutilizable que centraliza la lógica de fallback para los consumidores que parten de un Post ya obtenido (AuthorCard, PostHero indirectamente, JSON-LD).

Exportar `type NormalizedAuthor` con la unión de campos que los consumidores necesitan (superset de lo que pide AuthorCard + author page): `{ id: string; name?: string | null; slug?: string | null; jobTitle?: string | null; bio?: string | null; avatar?: unknown; socialMedia?: { linkedin?, github?, twitter?, website? } | null; expertise?: Array<{ topic?: string }> | null; education?: unknown[] | null; experience?: unknown[] | null; role?: string | null }`. El campo `avatar` se deja como el objeto media resuelto (o id) tal cual viene, para que `AuthorCard`/author page lo consuman igual que hoy.

Exportar `resolvePostAuthors(post): NormalizedAuthor[]`. Lógica: si `post.postAuthors` existe y tiene al menos un elemento resuelto como objeto (typeof === 'object'), mapear esos docs de la colección `authors` a `NormalizedAuthor`. Si `postAuthors` está vacío/ausente O sus elementos son solo ids sin resolver, hacer FALLBACK a `post.authors` (los User docs) mapeándolos con la MISMA forma. En ambos casos filtrar entradas nulas y normalizar a la misma shape para que el render sea idéntico independientemente de la fuente. El helper es puro (no hace queries) — opera sobre el post ya resuelto por depth; documentar en JSDoc que el caller debe fetchear el post con depth ≥ 1 (los callers actuales usan depth 1–2, ya cubierto).

NO importar tipos que fuercen a una sola colección; usar un tipo laxo (`post: { postAuthors?: unknown[] | null; authors?: unknown[] | null }`) para aceptar tanto User como Author sin fricción de types. Añadir un cast interno mínimo y comentado.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep "resolvePostAuthors" || echo "helper compiles clean"</automated>
  </verify>
  <done>`resolvePostAuthors` y `NormalizedAuthor` exportados; prefiere postAuthors, cae a authors(users); forma normalizada única; compila sin errores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cutover de las 11 superficies de lectura + hook populateAuthors</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/(frontend)/[locale]/author/[slug]/page.tsx, src/app/(frontend)/[locale]/authors/page.tsx, src/app/(frontend)/(sitemaps)/authors-sitemap.xml/route.ts, src/app/(frontend)/sitemap/page.tsx, src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx, src/components/AuthorCard/index.tsx, src/utilities/generateSchema.ts, src/components/admin/LiveUrlLink.tsx, src/collections/Posts/hooks/populateAuthors.ts</files>
  <action>
Cierra AUTHORS-03. Todas las lecturas PREFIEREN Authors y CAEN a users. Cambiar TODO en este commit (lecturas + hook en lockstep, RESEARCH §D). Regla transversal: nunca 500 por fallback; envolver queries nuevas en try/catch como ya hacen las funciones existentes.

Superficie #1 — `[locale]/authors/[slug]/page.tsx` (ISR canónica):
  - `generateStaticParams`: obtener slugs de `authors` (`find authors, select slug`) UNIDOS con los de `users` (dedupe por slug) para no perder rutas si la migración aún no corrió.
  - `queryUserBySlug` → renombrar conceptualmente a resolver-por-slug: primero `find({ collection: 'authors', where: { slug: { equals } }, depth: 2, locale })`; si vacío, fallback al `find users` actual (por slug o id). Devolver el doc + una bandera de fuente (`source: 'authors' | 'users'`).
  - `queryPostsByAuthor`: si la fuente es `authors`, query `where: { postAuthors: { contains: authorId } }`; si la fuente es `users`, mantener `where: { authors: { contains: userId } }`. Conservar el `select`/`depth`/`_status` actuales (PERF-01 intacto).
  - El render del avatar/social/expertise/education/experience ya funciona con la misma shape (Authors replica los campos verbatim) — no tocar el JSX salvo que los nombres de campo difieran (no difieren).

Superficie #2 — `[locale]/author/[slug]/page.tsx` (legacy singular): mismo patrón que #1: resolver por slug contra `authors` con fallback a `users` (por slug, luego id); posts por `postAuthors contains` (fuente authors) o `authors contains` (fuente users). Mantener el JSON-LD (`generatePersonSchema`) leyendo del doc resuelto.

Superficie #3 — `[locale]/authors/page.tsx` (listado): en `getAuthors`, `find({ collection: 'authors', limit: 100 })`; si `res.docs` está vacío, fallback a `find users`. El tipo `AuthorRef` local ya cubre name/slug/avatar/role — sin cambio de render.

Superficie #4 — `(sitemaps)/authors-sitemap.xml/route.ts`: dentro de `getAuthorsSitemap`, `find({ collection: 'authors', where: { slug: { exists: true } }, depth: 0, select: { slug, updatedAt } })`; si `results.docs.length === 0`, fallback al `find users` actual (mismo where/select). El resto (buildAlternateRefs, esLoc/enLoc `/authors/{slug}`, tags) NO cambia → URLs idénticas garantizadas por slug verbatim. Mantener el `unstable_cache` tag `authors-sitemap`.

Superficie #5 — `(frontend)/sitemap/page.tsx` (~L76, "Authors (Users)"): cambiar el `find users` por `find authors`; si vacío, fallback a `find users`. Ajustar el comentario. El bloque que renderiza el grupo de autores usa name/slug — sin cambio de shape.

Superficie #6 — `blog/[category]/[slug]/page.tsx` (~L195) + `AuthorCard`:
  - Reemplazar el uso directo de `post.authors[0]` por `resolvePostAuthors(post)[0]` (import del helper de Task 2). El post ya se fetchea a depth 2, así que `postAuthors` viene resuelto.
  - `AuthorCard` (`src/components/AuthorCard/index.tsx`): cambiar el prop type de `Pick<User, ...>` a `NormalizedAuthor` (import del helper). El JSX no cambia (mismos campos name/jobTitle/bio/avatar/slug/socialMedia). Verificar que `avatar` sigue tratándose como objeto media con `.url`.

Superficie #7 — `heros/PostHero/index.tsx`: consume `post.populatedAuthors` vía `formatAuthors`. NO cambiar este archivo directamente; queda cubierto al actualizar el hook `populateAuthors` (abajo), que sigue poblando `populatedAuthors` con `{ id, name, slug }`. `formatAuthors` y el link `/authors/{slug}` siguen válidos.

Superficie #8 — `utilities/generateSchema.ts` (~L58): hoy `const authors = doc.populatedAuthors || doc.authors`. Mantener esa línea (populatedAuthors se sigue poblando por el hook actualizado). No requiere cambio funcional si el hook prefiere postAuthors; el fallback hardcodeado a 'Juan Carlos Angulo' se conserva como red de seguridad.

Superficie #9 — `blocks/PostArticleHeader/config.ts`: FUERA DE SCOPE (RESEARCH Pitfall 5, CONTEXT §8). Es un campo `author relationTo: 'users'` independiente de Posts.authors; re-apuntarlo requiere su propia migración de bloques. Dejar en `users` y anotar como deferred a Phase 58. NO tocar.

Superficie #10 — `components/admin/LiveUrlLink.tsx` (~L15): hoy detecta `path.includes('users')` → basePath `/authors/`. Añadir detección de `path.includes('authors')` (la nueva colección) mapeando también a basePath `/authors/`, manteniendo la rama `users` para el liveUrl del perfil de user hasta Phase 58. Cambio aditivo, no destructivo.

Superficie #11 — `utilities/seo/keywordCoverageAudit.ts` + `scripts/*`: herramientas internas que consultan `collection: 'users'`. FUERA DE SCOPE del cutover de paridad (no afectan author page ni sitemap). Dejar en `users`; anotar como deferred a Phase 58.

Hook `populateAuthors` (`src/collections/Posts/hooks/populateAuthors.ts`): actualizar en lockstep. Preferir `doc.postAuthors` sobre `doc.authors`: si `postAuthors` tiene elementos, iterar esos ids con `findByID({ collection: 'authors', depth: 0 })`; si está vacío, iterar `doc.authors` con `findByID({ collection: 'users', depth: 0 })` (comportamiento actual). Emitir la MISMA shape `{ id, name, slug }` en ambos casos para no romper `formatAuthors`/PostHero/generateSchema. Envolver en try/catch como ya está.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -vE "^$" | grep -E "authors|AuthorCard|populateAuthors|generateSchema|sitemap|LiveUrlLink" || echo "cutover compiles clean"</automated>
  </verify>
  <done>Las 9 superficies en scope (#1-8, #10) leen Authors con fallback a users; #9 y #11 anotadas como deferred; hook prefiere postAuthors→authors emitiendo shape estable; tsc sin errores nuevos; URLs del sitemap idénticas por slug verbatim. (AUTHORS-03)</done>
</task>

<task type="auto">
  <name>Task 4: Scripts migrate-authors + verify-authors-collection + npm scripts</name>
  <files>src/scripts/migrate-authors.ts, src/scripts/verify-authors-collection.ts, package.json</files>
  <action>
RUN de ambos scripts DIFERIDO a Juan (necesitan `DATABASE_URI` viva). Solo se implementan y se dejan listos. Clonar el patrón de `src/scripts/migrate-blog-listing-to-page.ts` (dotenv → `getPayload({ config })` → `overrideAccess: true` → `context: { disableRevalidate: true }` → deja la fuente INTACTA → `process.exit`).

`src/scripts/migrate-authors.ts` (idempotente):
  1. Seleccionar Users a migrar = UNIÓN de (a) `find users where slug exists` y (b) users referenciados por cualquier post: paginar `posts` (batches de 50, `depth: 0`) y recolectar ids de `post.authors`. Dedupe por user id. (Hoy ~1: `juan-carlos-angulo`.)
  2. Por cada user (leerlo con `depth: 0` en locale `es` y luego `en` para los campos localizados): `upsert` en `authors` por slug (idempotente): buscar `find authors where slug equals user.slug`; si existe → `update`, si no → `create`. Setear `slug` VERBATIM del user (slugField respeta value explícito → paridad de URL). Copiar name, role, jobTitle, bio, expertise, socialMedia, education, experience, avatar (id del media), y los campos meta/SEO. Escribir AMBOS locales en dos llamadas: crear/actualizar `locale: 'es'`, luego `update` `locale: 'en'` sobre el MISMO doc (Payload escribe un locale por llamada, RESEARCH §C). Todas con `overrideAccess: true` y `context: { disableRevalidate: true }`.
  3. Construir el mapa `oldUserId → newAuthorId`.
  4. Backfill de `Posts.postAuthors`: paginar posts (batches de 50); para cada post con `authors.length > 0`, setear `postAuthors = post.authors.map(id → map[String(id)])` (filtrando ids sin mapeo) vía `payload.update` con `disableRevalidate`. Idempotente (re-setea el mismo valor).
  5. Loguear un RESULT JSON: `{ usersMigrated, authorsUpserted, postsBackfilled, postsMissingMapping }`. NO tocar ni borrar `users`.

`src/scripts/verify-authors-collection.ts` (espejo de `src/scripts/verify-authors.ts`):
  - Paginar posts publicados (batches de 50, `depth: 0`). Contar posts con `authors.length > 0` pero `postAuthors` vacío → debe ser 0.
  - Contar Authors creados vs users elegibles (deben coincidir).
  - Para cada Author, confirmar que `/authors/{author.slug}` equivale a un slug que ya existía en users (comparar sets de slugs). Loguear `{ totalPublished, postsWithOldAuthorMissingPostAuthors, authorsCount, eligibleUsersCount, slugParityOk }`. `process.exit(0)`.

`package.json`: añadir junto a `migrate:blog-listing` (~L18):
  `"migrate:authors": "tsx -r dotenv/config src/scripts/migrate-authors.ts"`
  `"verify:authors": "tsx -r dotenv/config src/scripts/verify-authors-collection.ts"`
  (Nota: existe ya un `verify-authors.ts` distinto; el nuevo npm script apunta al `-collection` para no colisionar.)
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "migrate-authors|verify-authors-collection" || echo "scripts compile clean"; grep -E "migrate:authors|verify:authors" package.json</automated>
  </verify>
  <done>Ambos scripts compilan, son idempotentes, usan Local API + disableRevalidate, no tocan users; npm scripts `migrate:authors` y `verify:authors` presentes. RUN diferido a Juan.</done>
</task>

<task type="auto">
  <name>Task 5: Gate de verificación de tipos y diff acotado</name>
  <files>src/payload-types.ts</files>
  <action>
Gate final (código-only; paridad visual/SEO y el RUN de la migración se difieren a Juan).

(1) Correr `pnpm exec tsc --noEmit` y confirmar CERO nuevos errores en `src/` respecto al baseline (los archivos tocados en Tasks 1–4). Si aparece un error nuevo, arreglarlo antes de cerrar.

(2) Correr `pnpm generate:types` de nuevo (idempotente) y confirmar `git diff src/payload-types.ts` muestra ÚNICAMENTE: la interface `Author`, la entrada `authors` en `Config['collections']`/`Config['collectionsJoins']` si aplica, y el campo `postAuthors` en `Post`. Cualquier otra diferencia es señal de un cambio de schema no intencionado — investigar.

(3) Dejar registrado en el SUMMARY el DEPLOY-ORDERING de RESEARCH §F adaptado a la estrategia aditiva-con-fallback:
   - Paso A (este plan, un solo deploy): schema Authors + postAuthors + cutover con fallback. Seguro con o sin migración (el fallback cubre el hueco).
   - Paso B (diferido a Juan, post-deploy): `pnpm migrate:authors` contra prod → luego `pnpm verify:authors` (0 posts sin postAuthors, paridad de slugs/URLs).
   - Paso C (Phase 58): retirar `authors→users`, el fallback, y limpiar `populatedAuthors` si queda huérfano.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | tail -5; echo "---"; git diff --stat src/payload-types.ts</automated>
  </verify>
  <done>tsc --noEmit sin nuevos errores en src/; diff de payload-types.ts limitado a Author + postAuthors; deploy-ordering documentado en el SUMMARY con los RUNs diferidos a Juan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| público → colección Authors (read) | `Authors.read: () => true` expone contenido de autor al público — por diseño (E-E-A-T). NO hereda el `read` bloqueado de `users` porque no contiene email/password. |
| script de migración → MongoDB prod | El script reescribe docs de producción (crea Authors, backfillea postAuthors). Corre con `overrideAccess: true`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Information Disclosure | `Authors.read: () => true` | accept | Colección sin datos de auth (email/password NO se migran, RESEARCH Security V4). Solo se exponen name/bio/avatar/social ya públicos hoy vía la author page. |
| T-56-02 | Tampering | `migrate-authors.ts` sobre prod | mitigate | Idempotente (upsert por slug), `disableRevalidate`, NO toca/borra `users` (rollback vivo). RUN manual y controlado por Juan con verify posterior. |
| T-56-03 | Denial of Service | cutover de lecturas cae a 500 si Authors vacío | mitigate | Cada query nueva tiene fallback a `users` + try/catch (patrón existente); ISR reintenta en la próxima revalidación (Issue #87). |
| T-56-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — esta fase NO instala paquetes (RESEARCH: sin dependencias nuevas). No hay checkpoint de legitimidad requerido. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` sin nuevos errores en `src/`.
- `pnpm generate:types` produce un diff acotado a `Author` + `postAuthors`.
- `Authors` aparece en el admin bajo "Contenido" (verificación visual → diferida a Juan).
- `authors-sitemap.xml` genera las mismas URLs (diff antes/después → diferido a Juan tras la migración).
- Diferido a Juan (DB viva): `pnpm migrate:authors` → `pnpm verify:authors` (0 posts sin postAuthors, paridad de slugs).
</verification>

<success_criteria>
Mapeo a los 4 criterios del ROADMAP + AUTHORS-01/02/03:
1. (Criterio 1 / AUTHORS-01) `Authors` en admin bajo "Contenido", editable con los campos para reconstruir la author page → Task 1.
2. (Criterio 2 / AUTHORS-02) Un Post se relaciona con ≥1 Author vía `postAuthors`, persiste al guardar → Task 1.
3. (Criterio 3 / AUTHORS-03) La author page muestra la misma info, ahora leída desde Authors (con fallback) → Tasks 2+3.
4. (Criterio 4 / AUTHORS-03) `authors-sitemap` genera las mismas URLs desde Authors (con fallback) → Task 3.
- Migración + verificación listas (RUN diferido) → Task 4.
- Gate de tipos verde y diff acotado → Task 5.
</success_criteria>

<output>
Create `.planning/phases/56-coleccion-authors/56-01-SUMMARY.md` when done.
</output>
