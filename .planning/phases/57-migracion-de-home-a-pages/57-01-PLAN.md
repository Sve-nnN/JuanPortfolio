---
phase: 57-migracion-de-home-a-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Pages/index.ts
  - src/payload-types.ts
  - src/app/(frontend)/[locale]/[slug]/page.tsx
  - src/app/(frontend)/[locale]/home/HomePage.tsx
  - src/utilities/generatePreviewPath.ts
  - src/collections/Pages/hooks/revalidatePage.ts
  - src/scripts/migrate-home-to-page.ts
  - package.json
autonomous: true
requirements: [PAGES-03]

must_haves:
  truths:
    - "La rama slug==='home' de [slug]/page.tsx lee la Page 'home' via getCachedPageBySlug con fallback al global 'home' cuando la Page no existe"
    - "generateMetadata (home) lee la Page 'home' con fallback al global, preservando hreflang/canonical/lang"
    - "Pages content.layout acepta FAQ y TestimonialsCarousel (bloques que usa home.layout)"
    - "El live preview de la Page 'home' apunta a '/' (o '/en'), no a '/home'"
    - "Existe src/scripts/migrate-home-to-page.ts idempotente y npm script migrate:home (RUN diferido a Juan)"
    - "El render visual (JsonLd isHome + HomePage + hero) y el gating de draftMode se preservan exactamente"
  artifacts:
    - path: "src/collections/Pages/index.ts"
      provides: "content.layout con FAQ + TestimonialsCarousel anadidos"
      contains: "FAQ"
    - path: "src/app/(frontend)/[locale]/[slug]/page.tsx"
      provides: "cutover de las 2 ramas home a Pages con fallback al global"
      contains: "getCachedPageBySlug"
    - path: "src/app/(frontend)/[locale]/home/HomePage.tsx"
      provides: "HomePage acepta layout directo, preserva normalizacion legacy"
      contains: "layout"
    - path: "src/utilities/generatePreviewPath.ts"
      provides: "caso especial pages+home -> '/'"
      contains: "home"
    - path: "src/scripts/migrate-home-to-page.ts"
      provides: "migracion idempotente global home -> Page slug home (ambos locales)"
      contains: "disableRevalidate"
  key_links:
    - from: "src/app/(frontend)/[locale]/[slug]/page.tsx"
      to: "src/utilities/getPages.ts (getCachedPageBySlug)"
      via: "import + llamada en rama home"
      pattern: "getCachedPageBySlug\\('home'"
    - from: "src/app/(frontend)/[locale]/[slug]/page.tsx"
      to: "payload.findGlobal({slug:'home'})"
      via: "fallback cuando la Page no existe"
      pattern: "findGlobal"
    - from: "src/collections/Pages/index.ts"
      to: "src/blocks/FAQ/config, src/blocks/TestimonialsCarousel/config"
      via: "import + entrada en blocks array"
      pattern: "FAQ|TestimonialsCarousel"
---

<objective>
Migrar la Home (`/` es, `/en` en) para que lea su contenido desde una entrada editable de `Pages` (slug `home`, campo `content.layout`) en lugar del global `home`, SIN regresionar ruteo, ISR/edge-cache, hreflang/canonical/lang ni live preview. Cubre PAGES-03. Es la superficie de MAYOR riesgo del milestone (la ruta `/`).

Purpose: Consolidar el contenido de la Home en `Pages` (patron validado en Phases 54/55) para poder retirar el global `home` en Phase 58.

Output: cutover de las 2 ramas `slug==='home'` con FALLBACK al global (deploy unico seguro), bloques faltantes anadidos a Pages, live preview + revalidacion ajustados, y script de migracion idempotente (RUN diferido a Juan). El global `home` queda INTACTO (rollback hasta Phase 58).

Nota de tamano: este plan tiene mas de 2-3 tasks a proposito. El cambio debe shippear de forma ATOMICA en un solo deploy sobre la ruta `/`; partirlo en olas introduciria estados intermedios peligrosos en la superficie de mayor riesgo. Cada task es una edicion pequena y contenida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-migracion-de-home-a-pages/57-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Contratos que el executor necesita. Extraidos del codebase. No hace falta explorar. -->

getCachedPageBySlug — src/utilities/getPages.ts
  export const getCachedPageBySlug = (slug, depth=2, locale?) => () => Promise<Page|null>
  // unstable_cache, tag: `pages_${slug}`; overrideAccess:false (solo publicados)

Patron de referencia YA en produccion (54/55) — src/app/(frontend)/[locale]/blog/page.tsx:
- Draft branch (cookie-gated, uncached, overrideAccess:true, draft:true) via react cache()
  para live preview; public branch usa exclusivamente getCachedPageBySlug (preserva ISR/HIT).
- generateMetadata: getCachedPageBySlug('blog',2,locale)().catch(()=>null) -> generateMeta({doc,locale,path}).
- generateMeta es source-agnostic (global vs page): hreflang/canonical se mantienen correctos.

Rama home ACTUAL a reemplazar — src/app/(frontend)/[locale]/[slug]/page.tsx:
- Componente Page L89-113: payload.findGlobal({slug:'home',depth:2,draft,locale}) -> render
  <main className="pb-24"><JsonLd isHome blocks={homeGlobal.layout} .../><PageClient/>
   <PayloadRedirects disableNotFound url/>{draft&&<LivePreviewListener/>}<HomePage homeGlobal locale/></main>
- generateMetadata L169-176: payload.findGlobal({slug:'home',locale}) -> generateMeta({doc,locale}).

HomePage ACTUAL — src/app/(frontend)/[locale]/home/HomePage.tsx:
- ({ homeGlobal: Home, locale }); extrae layout = homeGlobal.layout; normaliza legacy
  localized layout (si es objeto -> layout[locale]||layout.es||[]); si no hay layout muestra
  guia de setup; si hay -> <RenderBlocks blocks={layout} locale={locale}/>.

Home global blocks — src/globals/Home/config.ts (18): HeroHome, AboutSection, AboutWithFeatures,
  FeaturedWorks, FeaturedClients, FeaturedBlog, FAQ, FeaturedBlogPosts, FeaturedCaseStudies,
  ContactFormBlock, TestimonialSection, ResultsSection, LatestBlogPosts, LatestCaseStudies,
  TestimonialsCarousel, CalendlyEmbed, CallToAction, Content.

Pages content.layout blocks — src/collections/Pages/index.ts:153-190 (ya incluye CalendlyEmbed L189,
  HeroHome, AboutSection, AboutWithFeatures, FeaturedWorks, FeaturedClients, FeaturedBlog,
  ContactFormBlock, FeaturedBlogPosts, FeaturedCaseStudies, TestimonialSection, ResultsSection,
  LatestBlogPosts, LatestCaseStudies, CallToAction, Content, ...listados/single/Section).
  GAP CONFIRMADO vs Home global: FALTAN FAQ y TestimonialsCarousel. CalendlyEmbed YA presente.

Script de migracion de referencia — src/scripts/migrate-blog-listing-to-page.ts
  (findGlobal depth:0 por locale -> upsert por slug -> es create/update, en update; disableRevalidate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cerrar el gap de bloques en Pages content.layout (FAQ + TestimonialsCarousel)</name>
  <files>src/collections/Pages/index.ts, src/payload-types.ts</files>
  <action>
Gap ya confirmado durante el planning cruzando src/globals/Home/config.ts contra src/collections/Pages/index.ts:153-190. Faltan EXACTAMENTE dos bloques que usa home.layout: FAQ y TestimonialsCarousel. CalendlyEmbed YA esta presente (L189) — NO re-agregar.

Antes de editar, verificar que ambos modulos exportan el simbolo esperado: `grep -n "export const FAQ" src/blocks/FAQ/config.ts` y `grep -n "export const TestimonialsCarousel" src/blocks/TestimonialsCarousel/config.ts`.

En src/collections/Pages/index.ts: (a) anadir los imports `import { FAQ } from '../../blocks/FAQ/config'` y `import { TestimonialsCarousel } from '../../blocks/TestimonialsCarousel/config'` junto al resto de imports de bloques (cerca de L44-45). (b) Anadir `FAQ,` y `TestimonialsCarousel,` al array content.layout.blocks (L153-190), agrupados con los bloques de Home (por ejemplo despues de FeaturedBlog o junto a TestimonialSection; orden estetico libre; no reordenar bloques existentes).

Regenerar tipos: `pnpm generate:types`. El diff en src/payload-types.ts debe quedar ACOTADO a las nuevas variantes de bloque dentro del union de pages.content.layout (aparicion de los blockTypes de FAQ y TestimonialsCarousel). Si el diff toca otros tipos no relacionados, detenerse e investigar antes de continuar.

Cubre PAGES-03 (precondicion: sin estos bloques, Payload rechazaria el doc `home` migrado que contenga FAQ/TestimonialsCarousel).
  </action>
  <verify>
    <automated>grep -q "FAQ" "src/collections/Pages/index.ts" && grep -q "TestimonialsCarousel" "src/collections/Pages/index.ts" && grep -q "TestimonialsCarousel" src/payload-types.ts && echo OK</automated>
  </verify>
  <done>Pages content.layout importa y lista FAQ y TestimonialsCarousel; generate:types corrio y el diff de src/payload-types.ts se limita a los bloques anadidos; CalendlyEmbed no se duplico.</done>
</task>

<task type="auto">
  <name>Task 2: Cutover de lectura — las 2 ramas home de [slug]/page.tsx con fallback al global + refactor minimo de HomePage</name>
  <files>src/app/(frontend)/[locale]/[slug]/page.tsx, src/app/(frontend)/[locale]/home/HomePage.tsx</files>
  <action>
Reescribir las DOS ramas `if (slug === 'home')` de [slug]/page.tsx reusando el patron 54/55, pero con un FALLBACK mas fuerte: cuando la Page `home` no existe, leer el global `home` para que `/` renderice IDENTICO pre-migracion. Ese fallback es lo que hace seguro un deploy unico. Preservar EXACTAMENTE el path de render y el gating de draftMode.

HomePage primero (opcion de MENOR riesgo elegida: refactor liviano de firma, NO shim):
Cambiar la firma de HomePage de `({ homeGlobal: Home, locale })` a `({ layout, locale }: { layout: unknown; locale?: 'en' | 'es' })`. Dentro, `let layout = props.layout` reemplaza `homeGlobal.layout`. PRESERVAR intacta la normalizacion legacy-localized-layout (el bloque que convierte un layout-objeto en `layout[locale]||layout.es||[]`) y la guia de setup cuando no hay bloques. El render `<RenderBlocks blocks={layout as any} locale={locale}/>` no cambia. Razon de elegir refactor sobre shim `{layout} as Home`: evita una mentira de tipos sobre un objeto parcial y deja la normalizacion legacy en un solo lugar reutilizado por ambos paths (Page y fallback global).

Componente Page (rama L89-113):
1. `const { isEnabled: draft } = await draftMode()` ya esta arriba (L83). Anadir una lectura draft-aware para live preview (mismo patron que blog): en draft, un `queryHomePageDraft(locale)` con react `cache()`, `payload.find({collection:'pages', where:{slug:{equals:'home'}}, draft:true, depth:2, overrideAccess:true, pagination:false, locale})` -> `docs?.[0] ?? null`.
2. `const page = draft ? await queryHomePageDraft(locale).catch(()=>null) : await getCachedPageBySlug('home',2,locale)().catch(()=>null)`.
3. Derivar el layout con FALLBACK: `let layout = page?.content?.layout`. Si `page` es null (migracion no corrida), leer el global: `payload.findGlobal({slug:'home', depth:2, draft, locale})` y usar `homeGlobal.layout`. Consolidar en una unica variable `layout`.
4. Render sin cambios estructurales: `<main className="pb-24"><JsonLd isHome blocks={layout} locale={locale} siteUrl={getServerSideURL()}/><PageClient/><PayloadRedirects disableNotFound url={url}/>{draft && <LivePreviewListener/>}<HomePage layout={layout} locale={locale}/></main>`. Mantener el `url` actual (`(locale==='es'?'':'/'+locale)+'/'+slug`).

generateMetadata (rama L169-176):
Reemplazar `payload.findGlobal({slug:'home',locale})` por: `const page = await getCachedPageBySlug('home',2,locale)().catch(()=>null)`; si `page` es null, fallback `const doc = await payload.findGlobal({slug:'home',locale})`; en otro caso `doc = page`. `return generateMeta({ doc: doc as any, locale })`. generateMeta es source-agnostic (validado en blog) -> hreflang/canonical/lang se mantienen. NO anadir `path` para no cambiar el canonical actual de la home. NO tocar las ramas blog-listing / case-studies-listing.

No introducir `no-store` ni `force-dynamic`. La rama publica debe permanecer exclusivamente sobre `getCachedPageBySlug` (cacheada por tag) para preservar ISR / x-vercel-cache HIT. El global solo se toca en el path de fallback (Page ausente); el path draft nunca toca el global.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "src/app/.frontend./.locale." | grep -v node_modules | head; grep -q "getCachedPageBySlug('home'" "src/app/(frontend)/[locale]/[slug]/page.tsx" && grep -q "findGlobal" "src/app/(frontend)/[locale]/[slug]/page.tsx" && grep -q "layout={layout}" "src/app/(frontend)/[locale]/[slug]/page.tsx" && echo OK</automated>
  </verify>
  <done>Ambas ramas home leen la Page 'home' con fallback al global; HomePage acepta `layout` directo y preserva la normalizacion legacy; render y draftMode intactos; tsc sin errores NUEVOS en las rutas [slug]/home.</done>
</task>

<task type="auto">
  <name>Task 3: Live preview, generateStaticParams y revalidacion para la Page home -> '/'</name>
  <files>src/utilities/generatePreviewPath.ts, src/collections/Pages/hooks/revalidatePage.ts, src/app/(frontend)/[locale]/[slug]/page.tsx</files>
  <action>
(a) generatePreviewPath (src/utilities/generatePreviewPath.ts): la Page `home` debe previsualizarse en `/` (o `/en`), no en `/home`. Hoy arma `path = `${localePrefix}${collectionPrefixMap[collection]}/${slug}``. Anadir un caso especial ANTES de construir `encodedParams`: si `collection === 'pages' && slug === 'home'` entonces `path = localePrefix || '/'` (es -> `/`, en -> `/en`). El resto (previewSecret, ruta `/next/preview`) sin cambios. draftMode ya esta en la rama del route.

(b) generateStaticParams (src/app/(frontend)/[locale]/[slug]/page.tsx): CONFIRMAR (no cambiar salvo que sea necesario) que la logica actual sigue emitiendo `home` exactamente una vez tras la migracion. El filtro L48-53 excluye `home`/`blog`/`case-studies` del loop de pages, y L58-61 hace push manual de `home` para ambos locales. Como `home` ahora existe como Page, el filtro sigue excluyendolo del loop => el push manual lo agrega una sola vez por locale. Verificar por lectura que NO se duplica. Ajustar SOLO si se detecta doble-add (no se espera).

(c) revalidatePage (src/collections/Pages/hooks/revalidatePage.ts): el hook YA mapea `home -> '/'` (L14) y hace `revalidateTag('pages_' + doc.slug)` => la etiqueta `pages_home` invalida el read cacheado usado por AMBOS locales de la home. Belt-and-suspenders para la ruta `/en`: en el caso `home`, ademas de `revalidatePath('/')`, anadir `revalidatePath('/en')` para forzar refresco del HTML ISR de la home EN. Aplicar el mismo par en revalidateDelete y en la rama de previousDoc (cuando `previousDoc.slug === 'home'`). No cambiar el comportamiento para el resto de slugs.

Cubre PAGES-03 SC4 (live preview) y SC2 (ISR/cache tras editar).
  </action>
  <verify>
    <automated>grep -q "home" "src/utilities/generatePreviewPath.ts" && grep -q "'/en'" "src/collections/Pages/hooks/revalidatePage.ts" && pnpm exec tsc --noEmit 2>&1 | grep -E "generatePreviewPath|revalidatePage" | grep -v node_modules | head && echo DONE || echo DONE</automated>
  </verify>
  <done>generatePreviewPath devuelve `/` (es) o `/en` para pages+home; generateStaticParams confirmado emite home una sola vez por locale (documentado en SUMMARY); revalidatePage invalida `/` y `/en` al editar la Page home; sin errores tsc nuevos.</done>
</task>

<task type="auto">
  <name>Task 4: Script de migracion idempotente migrate-home-to-page.ts + npm script (RUN diferido a Juan)</name>
  <files>src/scripts/migrate-home-to-page.ts, package.json</files>
  <action>
Crear src/scripts/migrate-home-to-page.ts calcando el patron de src/scripts/migrate-blog-listing-to-page.ts, con `const SLUG = 'home'`:
1. Leer el global `home` por locale con depth 0: `payload.findGlobal({slug:'home', locale:'es', depth:0})` y `locale:'en'`.
2. Idempotencia: `payload.find({collection:'pages', where:{slug:{equals:'home'}}, limit:1, overrideAccess:true})`.
3. Mapeo global -> Page: `title` (required; usar `es.title` o un fallback como 'Inicio' / 'Home'), `slug: 'home'`, `_status: 'published'`, `content: { layout: es.layout ?? [] }`. Copiar `meta.description` desde el global SOLO si el global home tiene ese campo (verificar la forma del global antes; si no existe, omitir meta). NO poblar `hero` (la home usa HeroHome dentro de layout, igual que el listing usa ListingHero).
4. es: create si no existe / update si existe (mismo doc). en: `payload.update` del MISMO doc con `locale:'en'`, `content: { layout: en.layout ?? [] }` y title EN.
5. TODAS las escrituras con `overrideAccess: true` y `context: { disableRevalidate: true }`.
6. El global `home` queda INTACTO (rollback hasta Phase 58). Log final + `process.exit(0)`; catch -> `process.exit(1)`.

package.json: anadir `"migrate:home": "tsx -r dotenv/config src/scripts/migrate-home-to-page.ts"` junto a los otros scripts migrate:*.

IMPORTANTE: NO ejecutar el script. El RUN de `pnpm migrate:home` sobre la DB viva queda DIFERIDO a Juan (ver Deferred). El script solo debe existir, tipar y estar cableado.
  </action>
  <verify>
    <automated>test -f src/scripts/migrate-home-to-page.ts && grep -q "disableRevalidate" src/scripts/migrate-home-to-page.ts && grep -q "migrate:home" package.json && pnpm exec tsc --noEmit 2>&1 | grep "migrate-home-to-page" | grep -v node_modules | head && echo CHECK || echo CHECK</automated>
  </verify>
  <done>Existe migrate-home-to-page.ts idempotente (ambos locales, disableRevalidate, global intacto); npm script migrate:home cableado; tsc sin errores nuevos; script NO ejecutado.</done>
</task>

<task type="auto">
  <name>Task 5: Gate de verificacion — tsc limpio y diff de tipos acotado</name>
  <files>src/payload-types.ts</files>
  <action>
Gate final del plan (lo que Claude PUEDE verificar sin DB viva ni deploy):
1. `pnpm exec tsc --noEmit` -> sin errores NUEVOS en `src/` respecto al baseline (comparar contra el estado previo; los errores preexistentes ajenos a esta fase no cuentan).
2. `pnpm generate:types` -> el diff de src/payload-types.ts debe limitarse a las nuevas variantes de bloque (FAQ, TestimonialsCarousel) en el union de pages.content.layout. Cualquier cambio fuera de eso => investigar.
3. Documentar en el SUMMARY, como DIFERIDO a Juan (no verificable aqui): paridad visual pixel de `/` (es) y `/en`; `x-vercel-cache: HIT` sin `no-store` en ambas; hreflang/canonical/`<html lang>` correctos por locale; live preview de la Page `home` sobre `/`; y el RUN de `pnpm migrate:home`.
4. Recalcar en el SUMMARY que el FALLBACK al global hace el deploy UNICO seguro: con la Page `home` ausente, `/` sigue renderizando identico via el global; tras correr la migracion, pasa a leer la Page sin re-deploy.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && echo NO_TS_ERRORS || (echo "revisar errores nuevos vs baseline"; pnpm exec tsc --noEmit 2>&1 | grep "error TS" | head)</automated>
  </verify>
  <done>tsc sin errores nuevos en src/; diff de payload-types acotado a FAQ/TestimonialsCarousel; SUMMARY lista los checks diferidos a Juan y confirma el argumento del deploy unico seguro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante anonimo -> ruta `/` (SSR/ISR) | lectura publica; solo docs publicados (getCachedPageBySlug overrideAccess:false) |
| editor autenticado -> live preview (draftMode cookie) | rama draft con overrideAccess:true, cookie-gated, no cacheada |
| script de migracion -> DB | escritura con overrideAccess:true, disableRevalidate, ejecutado por Juan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Denial of Service | rama home de [slug]/page.tsx | mitigate | fallback al global cuando la Page es null (`.catch(()=>null)` en el read cacheado) evita 500 en `/` pre-migracion; deploy unico seguro |
| T-57-02 | Information Disclosure | render publico de `/` | mitigate | path publico usa exclusivamente getCachedPageBySlug (overrideAccess:false, solo publicados); drafts solo via rama draftMode cookie-gated |
| T-57-03 | Tampering | ISR/edge-cache de `/` y `/en` | mitigate | sin no-store/force-dynamic; revalidateTag('pages_home') + revalidatePath('/','/en') invalidan ambos locales al editar |
| T-57-04 | Elevation of Privilege | live preview path | accept | overrideAccess:true solo dentro de la rama draftMode (cookie bypass gated), patron ya en prod (54/55); riesgo bajo |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes nuevos; reusa modulos existentes del repo |
</threat_model>

<verification>
Verificable por Claude (gate del plan):
- `pnpm exec tsc --noEmit` sin errores nuevos en src/.
- `pnpm generate:types` con diff acotado a FAQ + TestimonialsCarousel.
- greps de key_links (getCachedPageBySlug('home', findGlobal fallback, layout={layout}, caso home en generatePreviewPath, '/en' en revalidatePage, disableRevalidate en el script).

Diferido a Juan (DB viva / deploy — no verificable aqui):
- RUN de `pnpm migrate:home`.
- Paridad visual pixel de `/` (es) y `/en`.
- `x-vercel-cache: HIT` sin `no-store` en `/` y `/en`.
- hreflang, canonical y `<html lang>` correctos por locale.
- Live preview de la Page `home` renderizando en `/`.
</verification>

<success_criteria>
Mapeo a los 4 SC de PAGES-03 (Phase 57):
- SC1 (paridad visual, ahora desde Pages): Task 2 (cutover con render identico) + Task 1 (bloques disponibles). Verificacion pixel diferida a Juan.
- SC2 (`/` mantiene x-vercel-cache HIT / ISR sin no-store): Task 2 (rama publica solo sobre getCachedPageBySlug, sin no-store) + Task 3c (revalidacion). Verificacion de headers diferida a Juan.
- SC3 (hreflang/canonical/`<html lang>` en `/` y `/en`): Task 2 (generateMetadata con generateMeta source-agnostic). Verificacion por locale diferida a Juan.
- SC4 (live preview sobre la Page de Pages): Task 3a (generatePreviewPath home -> `/`) + Task 2 (rama draft). Verificacion end-to-end diferida a Juan.

El FALLBACK al global `home` garantiza que un solo deploy es seguro: con la Page ausente `/` renderiza identico al estado actual; el cutover a Pages ocurre cuando Juan corre `pnpm migrate:home`, sin re-deploy.
</success_criteria>

<output>
Crear `.planning/phases/57-migracion-de-home-a-pages/57-01-SUMMARY.md` al terminar.
</output>
