# v1.9 — Acciones manuales de Juan

Pasos que el run autónomo NO puede ejecutar (necesitan DB en vivo, deploy, o verificación visual). Correr en orden al cerrar el milestone.

## ⚠️ ORDEN DE DEPLOY (crítico — evita romper producción)

El milestone se despliega en DOS etapas por seguridad. `main` (fases 52-57) es aditivo con fallbacks: deployar solo main NO puede romper nada. El branch `chore/v1.9-retire-globals` (fase 58, retiro destructivo de globals + fallbacks) SOLO se mergea/deploya después de migrar+verificar.

1. **Deploy de `main`** (fases 52-57) → seguro, todo cae a globals/users por fallback.
2. **Correr las 4 migraciones** contra la DB de producción (sección siguiente).
3. **Verificar** en prod (checklists de abajo) que `/`, `/blog`, `/case-studies` y `/authors` sirven desde Pages/Authors.
4. **Merge + deploy de `chore/v1.9-retire-globals`** → retira globals y fallbacks. Si se deploya ANTES del paso 2-3, home/blog/case-studies quedan vacíos y los posts pierden autor.

> Las migraciones (`migrate:*`) viven en `main`. Corrélas desde main ANTES de mergear el branch (el branch las borra como limpieza post-uso).

## Migraciones de datos (necesitan `DATABASE_URI` en vivo)
Correr contra la DB objetivo ANTES de validar cada superficie. Todas idempotentes; los globals quedan intactos hasta Phase 58 (rollback = revertir el commit del swap).

- [ ] **Phase 54 — blog listing:** `pnpm migrate:blog-listing`
      Crea/actualiza la Page slug `blog` con title/description/layout de ambos locales desde el global `blog-listing`.
      Hasta correrla, `/blog` degrada al fallback null-safe (no 500).
- [ ] **Phase 55 — case studies listing:** `pnpm migrate:case-studies-listing`
      Crea/actualiza la Page slug `case-studies` (ambos locales) desde el global `case-studies-listing`.
      Hasta correrla, `/case-studies` degrada al fallback null-safe (no 500).
- [ ] **Phase 56 — Authors:** `pnpm migrate:authors` y luego `pnpm verify:authors`
      Crea Authors desde los Users-autor (slug verbatim) y backfillea `Posts.postAuthors`.
      verify debe dar `postsWithOldAuthorMissingPostAuthors: 0`, `authorsCount == eligibleUsersCount`, `slugParityOk: true`.
      Deploy único es seguro corra o no la migración (lecturas caen a users por fallback).
- [ ] **Phase 57 — Home:** `pnpm migrate:home`
      Crea/actualiza la Page slug `home` con `layout` de ambos locales desde el global `home`.
      Deploy único es seguro corra o no: `/` cae al global `home` por fallback hasta migrar. Idempotente.

## Verificación visual en /admin (batch)
- [ ] **Phase 52 — nav:** abrir `/admin`, confirmar 4 grupos (Contenido / Marketing / SEO-Métricas / Sitio), nada suelto. Cambiar idioma a EN → Content/Site/SEO & Metrics/Marketing.
- [ ] **Phase 53 — Pages:** crear una Page, publicar, ver render en su ruta. Duplicar una Page → slug `-copy`, título ` (copy)`, independiente del original.

## Smoke de deploy por superficie migrada (post-deploy Vercel)
- [ ] **Phase 54 — /blog:** paridad visual es+en; `x-vercel-cache: HIT` (ISR sin no-store) en `/es/blog` y `/en/blog`; hreflang/canonical/`<html lang>` correctos; live preview de Payload sobre la Page `blog`.
- [ ] **Phase 55 — /case-studies:** paridad visual es+en; `x-vercel-cache: HIT` en `/es/case-studies` y `/en/case-studies`; hreflang/canonical/`<html lang>` correctos; live preview sobre la Page `case-studies`.
- [ ] **Phase 56 — Authors:** `/authors` y `/authors/{slug}` es+en; `authors-sitemap.xml` con mismas URLs; byline + JSON-LD de un post; Authors editable en admin bajo "Contenido".
- [ ] **Phase 57 — Home:** paridad pixel en `/` (es) y `/en`; `x-vercel-cache: HIT` sin no-store; hreflang/canonical/`<html lang>` por locale; live preview de la Page `home` en `/`.
