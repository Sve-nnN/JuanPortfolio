---
phase: 01-crawl-errors-indexability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/posts/seo/mejores-cursos-seo-espanol.md
autonomous: true
requirements:
  - CRAWL-01
  - CRAWL-02
  - CRAWL-05

must_haves:
  truths:
    - "GET /blog/seo/mejores-cursos-seo-espanol returns 200 (new canonical URL works)"
    - "GET /blog/seo/mejores-cursos-seo-en-español returns 301 → /blog/seo/mejores-cursos-seo-espanol"
    - "GET /blog/general returns a page with noindex in rendered HTML"
    - "The old ñ-slug URL no longer appears in the sitemap"
  artifacts:
    - path: "content/posts/seo/mejores-cursos-seo-espanol.md"
      provides: "Renamed post file with ascii slug (no ñ)"
      contains: "slug: mejores-cursos-seo-espanol"
    - path: "Payload Redirects collection (CMS record)"
      provides: "301 redirect: /blog/seo/mejores-cursos-seo-en-espa%C3%B1ol → /blog/seo/mejores-cursos-seo-espanol"
    - path: "Payload Categories collection (CMS record)"
      provides: "General category has noindex: true set"
  key_links:
    - from: "/blog/seo/mejores-cursos-seo-en-espa%C3%B1ol (old URL with ñ)"
      to: "/blog/seo/mejores-cursos-seo-espanol (new URL)"
      via: "Payload Redirects collection → fetch-redirects.ts → next.config.js redirects array"
      pattern: "mejores-cursos-seo-en-espa"
    - from: "General category record (CMS)"
      to: "<meta name=\"robots\" content=\"noindex\">"
      via: "generateMeta reads doc.noindex (top-level on Category per payload-types.ts line 556)"
      pattern: "noindex.*true"
---

<objective>
Rename the mejores-cursos slug (removing the ñ character), create a 301 redirect for the old URL, and apply noindex to the General category — all three via Payload MCP direct CMS operations plus a local file rename.

Purpose: The ñ character in the slug creates URL encoding risks across CDNs/proxies (some encode it, some don't), causing inconsistent crawl behavior and the 4xx error reported in the audit. The General category is empty content with zero ranking potential — noindexing it eliminates crawl budget waste.

Output: One renamed markdown file (mejores-cursos-seo-espanol.md), one new Redirects record in CMS, and the General category record updated with noindex: true.
</objective>

<execution_context>
@/Users/juan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crawl-errors-indexability/01-CONTEXT.md

<interfaces>
<!-- Payload Redirects plugin schema (from @payloadcms/plugin-redirects) -->
<!-- Collection slug: redirects -->
<!-- Fields: from (text), to (relationship: pages/posts OR url text), type (301|302) -->
<!-- The plugin's `from` field is the path WITHOUT domain: /blog/seo/... -->
<!-- For URL-encoded paths, use the percent-encoded form in the `from` field -->

<!-- Payload Categories collection — noindex field is TOP-LEVEL (not nested under meta) -->
<!-- per payload-types.ts line 556: noindex?: boolean | null -->
<!-- The seoPlugin adds noindex to categories as a direct field (not under a meta group) -->

<!-- How redirects work in this project: -->
<!-- 1. Payload stores redirect in DB -->
<!-- 2. src/scripts/fetch-redirects.ts queries payload.find({ collection: 'redirects' }) at build time -->
<!-- 3. Results are written to .next/redirects.json and merged into next.config.js redirects array -->
<!-- 4. For live effect without rebuild, Payload's PayloadRedirects component also handles them client-side -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename mejores-cursos file and update slug in frontmatter</name>
  <files>content/posts/seo/mejores-cursos-seo-espanol.md</files>
  <action>
    1. Rename the file:
       mv "content/posts/seo/mejores-cursos-seo-en-español.md" "content/posts/seo/mejores-cursos-seo-espanol.md"
       (Use the Bash tool with quoted paths to handle the ñ character safely)

    2. Read the renamed file and update the frontmatter slug field.
       Find the `slug` field in frontmatter. If it exists, change its value to `mejores-cursos-seo-espanol`.
       If the `slug` field does not exist (the file derives the slug from the filename), add it explicitly:
         slug: mejores-cursos-seo-espanol

    3. Do NOT change any other frontmatter fields or the body content — body content rewrite is Phase 2 (CONTENT-01), out of scope here.

    4. Verify the rename succeeded and the slug is correct:
       grep "slug" content/posts/seo/mejores-cursos-seo-espanol.md

    NOTE: The file currently has `description:` in frontmatter instead of `metaDescription:` (noted in 03-changes-applied.md). Leave this as-is — do not fix frontmatter field names here; that is Phase 2 scope.
  </action>
  <verify>
    <automated>test -f "content/posts/seo/mejores-cursos-seo-espanol.md" && ! test -f "content/posts/seo/mejores-cursos-seo-en-español.md" && echo "rename ok"</automated>
  </verify>
  <done>
    - content/posts/seo/mejores-cursos-seo-espanol.md exists
    - Old ñ-slug file no longer exists
    - slug field in frontmatter is mejores-cursos-seo-espanol
  </done>
</task>

<task type="auto">
  <name>Task 2: Payload MCP — rename CMS slug, create 301 redirect, noindex General category</name>
  <files></files>
  <action>
    Execute three Payload MCP operations in order:

    OPERATION A — Rename slug in CMS post:
    - Use MCP to find the post by slug mejores-cursos-seo-en-español (or its URL-encoded equivalent)
    - If found: update slug field to mejores-cursos-seo-espanol
    - If not found: try slug mejores-cursos-seo-espanol (may already have the right slug if push happened after rename)
    - Confirm: the CMS post for this article now has slug = mejores-cursos-seo-espanol

    OPERATION B — Create 301 redirect in Payload Redirects collection:
    Per the decisions in CONTEXT.md (use Payload Redirects plugin, not next.config):
    - Create a new record in the `redirects` collection with:
      - from: /blog/seo/mejores-cursos-seo-en-espa%C3%B1ol
      - to: { type: 'custom', url: '/blog/seo/mejores-cursos-seo-espanol' } OR as a reference to the post
      - type: 301
    - Also create a second redirect record for the non-encoded form (in case some crawlers send it unencoded):
      - from: /blog/seo/mejores-cursos-seo-en-español  (literal ñ)
      - to: /blog/seo/mejores-cursos-seo-espanol
      - type: 301
    - Confirm both redirect records were created successfully

    OPERATION C — Set noindex on General category:
    - Use MCP to find the category with slug = general
    - Update the record: set noindex = true (top-level field, not nested under meta)
    - Per payload-types.ts line 556, Categories have noindex as a direct top-level field
    - Confirm: the General category record now has noindex = true

    After all three operations, run pnpm sync push -- --post=seo/mejores-cursos-seo-espanol.md to ensure the renamed file is registered in content-sync.json with the new slug. Use --force if prompted about state mismatch.
  </action>
  <verify>
    <automated>grep -r "mejores-cursos-seo-espanol" content/content-sync.json && echo "slug registered in sync state"</automated>
  </verify>
  <done>
    - CMS post slug updated to mejores-cursos-seo-espanol (no ñ)
    - Two 301 redirect records exist in Payload Redirects (URL-encoded and literal ñ variants)
    - General category has noindex = true in CMS
    - content-sync.json contains an entry for mejores-cursos-seo-espanol
  </done>
</task>

</tasks>

<verification>
1. File check: content/posts/seo/mejores-cursos-seo-espanol.md exists, old ñ file does not
2. grep slug content/posts/seo/mejores-cursos-seo-espanol.md — shows mejores-cursos-seo-espanol
3. MCP: query redirects collection — confirm two 301 records for the ñ URLs exist
4. MCP: query categories where slug=general — confirm noindex = true
5. MCP: query posts where slug=mejores-cursos-seo-espanol — confirm record exists
6. grep mejores-cursos-seo-espanol content/content-sync.json — confirms sync state updated
</verification>

<success_criteria>
- Old ñ-slug file removed; new ascii-slug file exists with correct slug in frontmatter
- Payload CMS post record has slug = mejores-cursos-seo-espanol
- Two 301 redirect records cover both encoded and literal ñ URL variants
- General category CMS record has noindex = true
- content-sync.json updated with new slug
</success_criteria>

<output>
After completion, create `.planning/phases/01-crawl-errors-indexability/01-02-SUMMARY.md`
</output>
