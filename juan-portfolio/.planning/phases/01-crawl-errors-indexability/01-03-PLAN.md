---
phase: 01-crawl-errors-indexability
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - CRAWL-01
  - CRAWL-02
  - CRAWL-03
  - CRAWL-04
  - CRAWL-05

must_haves:
  truths:
    - "A fresh sitemap crawl returns 0 pages with 4xx or 3xx status"
    - "mejores-cursos-seo-espanol resolves 200 at the new URL; old ñ-slug returns 301"
    - "/blog/general has noindex in rendered HTML"
    - "/en/blog/seo/guia-eeat rendered HTML contains <meta name=\"robots\" content=\"noindex\">"
    - "/en/blog/cs-fundamentals/sql-vs-nosql rendered HTML contains <meta name=\"robots\" content=\"noindex\">"
    - "/en/blog/cs-fundamentals/experiencia-de-usuario rendered HTML contains noindex meta"
  artifacts:
    - path: ".planning/phases/01-crawl-errors-indexability/01-03-VERIFICATION.md"
      provides: "Written verification report with curl output and pass/fail for each success criterion"
  key_links:
    - from: "Plan 01-01 generateMeta change"
      to: "rendered <meta name=\"robots\"> in browser HTML"
      via: "pnpm build → Next.js static generation → HTML output"
      pattern: "robots.*noindex"
---

<objective>
Verify all Phase 1 success criteria are met in the running application: rendered HTML contains correct noindex tags, sitemap excludes noindexed pages, redirects work, and the new mejores-cursos URL resolves.

Purpose: Code and CMS changes from Plans 01-01 and 01-02 must be verified against the actual rendered output — not just source files. This plan confirms the fixes are end-to-end effective before marking Phase 1 complete.

Output: Verified pass/fail for each CRAWL-01 through CRAWL-05 requirement, plus a written VERIFICATION.md report.
</objective>

<execution_context>
@/Users/juan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crawl-errors-indexability/01-CONTEXT.md
@.planning/phases/01-crawl-errors-indexability/01-01-SUMMARY.md
@.planning/phases/01-crawl-errors-indexability/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run automated HTML verification checks</name>
  <files></files>
  <action>
    Run pnpm build to regenerate static pages with the changes from Plans 01-01 and 01-02 applied.
    If the build fails, diagnose and fix before continuing — do not proceed to verification with a broken build.

    After successful build, start pnpm start in the background and run the following curl checks.
    Use the dev server (pnpm dev) as an alternative if start is unavailable, but prefer built output.

    BASE_URL = http://localhost:3000

    CHECK 1 — CRAWL-05: New slug resolves 200:
      curl -o /dev/null -s -w "%{http_code}" "$BASE_URL/blog/seo/mejores-cursos-seo-espanol"
      Expected: 200

    CHECK 2 — CRAWL-05: Old ñ URL returns 301:
      curl -o /dev/null -s -w "%{http_code}" "$BASE_URL/blog/seo/mejores-cursos-seo-en-espa%C3%B1ol"
      Expected: 301

    CHECK 3 — CRAWL-04: guia-eeat EN has noindex:
      curl -s "$BASE_URL/en/blog/seo/guia-eeat" | grep -i 'name="robots"'
      Expected: contains noindex

    CHECK 4 — CRAWL-04: sql-vs-nosql EN has noindex:
      curl -s "$BASE_URL/en/blog/cs-fundamentals/sql-vs-nosql" | grep -i 'name="robots"'
      Expected: contains noindex

    CHECK 5 — CRAWL-03: experiencia-de-usuario EN has noindex:
      curl -s "$BASE_URL/en/blog/cs-fundamentals/experiencia-de-usuario" | grep -i 'name="robots"'
      Expected: contains noindex

    CHECK 6 — CRAWL-02: /blog/general has noindex:
      curl -s "$BASE_URL/blog/general" | grep -i 'name="robots"'
      Expected: contains noindex

    CHECK 7 — CRAWL-01: sitemap does not include noindexed pages (check the Next.js sitemap):
      curl -s "$BASE_URL/sitemap.xml" | grep -i "mejores-cursos-seo-en-espa"
      Expected: empty (old ñ URL not in sitemap)
      curl -s "$BASE_URL/sitemap.xml" | grep -i "guia-eeat"
      Note: guia-eeat ES should be present; verify guia-eeat EN is absent or confirm sitemap scope

    Record all outputs verbatim. Mark each check PASS or FAIL.

    If CHECK 3, 4, or 5 fail (noindex not in HTML): the generateMeta change in Plan 01-01 may not have compiled. Check if pnpm build ran after 01-01 changes. Re-run build.

    If CHECK 2 fails (301 not working): the redirect may need a production build to take effect via fetch-redirects.ts. Run: npx tsx src/scripts/fetch-redirects.ts and rebuild.
  </action>
  <verify>
    <automated>curl -o /dev/null -s -w "%{http_code}" http://localhost:3000/blog/seo/mejores-cursos-seo-espanol | grep -q "200" && echo "PASS: new URL resolves 200"</automated>
  </verify>
  <done>
    All 7 checks documented with actual output.
    Checks 1-6 each show PASS or FAIL with the raw curl output.
    Any FAIL items have a diagnosed root cause noted.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 complete — all crawl error and noindex fixes applied:
    - generateMeta.ts now emits robots: noindex when meta.noindex is true (Plan 01-01)
    - Sync module supports noindex frontmatter field end-to-end (Plan 01-01)
    - guia-eeat.en and sql-vs-nosql.en have meta.noindex=true in CMS (Plan 01-01)
    - experiencia-de-usuario.en.md stub created and synced with noindex (Plan 01-01)
    - mejores-cursos renamed to ascii slug + 301 redirect created (Plan 01-02)
    - General category has noindex=true in CMS (Plan 01-02)
    - Automated checks from Task 1 ran and results are documented
  </what-built>
  <how-to-verify>
    1. Start the dev server: pnpm dev
    2. Open each URL in your browser and use View Source (Cmd+U) to check for the robots meta tag:
       - http://localhost:3000/en/blog/seo/guia-eeat
         Look for: <meta name="robots" content="noindex" />
       - http://localhost:3000/en/blog/cs-fundamentals/sql-vs-nosql
         Look for: <meta name="robots" content="noindex" />
       - http://localhost:3000/en/blog/cs-fundamentals/experiencia-de-usuario
         Look for: <meta name="robots" content="noindex" />
       - http://localhost:3000/blog/general
         Look for: <meta name="robots" content="noindex" />

    3. Test the redirect:
       - Visit http://localhost:3000/blog/seo/mejores-cursos-seo-espanol — should load the page (200)
       - Use curl to test the 301 (browser follows redirects automatically):
         curl -I "http://localhost:3000/blog/seo/mejores-cursos-seo-en-espa%C3%B1ol"
         Should show: HTTP/1.1 301 and Location: .../mejores-cursos-seo-espanol

    4. Check sitemap exclusion:
       - Visit http://localhost:3000/sitemap.xml
       - Confirm: no URLs containing "espa%C3%B1ol" or "en-español"
       - Confirm: guia-eeat.en, sql-vs-nosql.en are absent (noindexed pages excluded)

    PASS: All 4 pages have noindex in source, redirect works, sitemap is clean.
    FAIL: Describe which check failed and the actual HTML/response you saw.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which items failed</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write VERIFICATION.md report</name>
  <files>.planning/phases/01-crawl-errors-indexability/01-03-VERIFICATION.md</files>
  <action>
    Write .planning/phases/01-crawl-errors-indexability/01-03-VERIFICATION.md documenting:

    ## Phase 1 Verification Report
    Date: [today]
    Status: COMPLETE / PARTIAL (based on checkpoint results)

    ### CRAWL-01: Zero 4xx/3xx in sitemap
    Status: PASS / FAIL
    Evidence: [sitemap curl output or manual check result]

    ### CRAWL-02: /blog/general has noindex
    Status: PASS / FAIL
    Evidence: [grep robots output from curl]

    ### CRAWL-03: experiencia-de-usuario EN noindexed
    Status: PASS / FAIL
    Evidence: [grep robots output from curl]

    ### CRAWL-04: guia-eeat.en and sql-vs-nosql.en noindexed
    Status: PASS / FAIL
    Evidence: [grep robots output from curl for both pages]

    ### CRAWL-05: mejores-cursos-seo-espanol resolves; ñ-slug returns 301
    Status: PASS / FAIL
    Evidence: [curl HTTP status codes]

    ### Any Remaining Issues
    [List any FAIL items with root cause and proposed fix, or "None" if all pass]

    Fill in all Status and Evidence fields from the actual outputs observed in Tasks 1 and 2.
    If the checkpoint was approved, all statuses should be PASS.
    If any were noted as failed in the checkpoint, record those as FAIL with the user's description.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-crawl-errors-indexability/01-03-VERIFICATION.md && echo "report exists"</automated>
  </verify>
  <done>
    01-03-VERIFICATION.md exists with PASS/FAIL for each of CRAWL-01 through CRAWL-05.
    All items show PASS (or FAIL items have root cause documented).
  </done>
</task>

</tasks>

<verification>
All five CRAWL requirements verified in rendered HTML:
- CRAWL-01: sitemap contains zero ñ URLs and zero noindexed post URLs
- CRAWL-02: /blog/general HTML contains name="robots" content including noindex
- CRAWL-03: /en/blog/cs-fundamentals/experiencia-de-usuario HTML contains noindex robots meta
- CRAWL-04: /en/blog/seo/guia-eeat and /en/blog/cs-fundamentals/sql-vs-nosql HTML contain noindex robots meta
- CRAWL-05: /blog/seo/mejores-cursos-seo-espanol returns 200; /blog/seo/mejores-cursos-seo-en-espa%C3%B1ol returns 301
</verification>

<success_criteria>
- pnpm build completes without errors after Plans 01-01 and 01-02 changes
- All 7 automated curl checks from Task 1 pass
- Human checkpoint approved
- 01-03-VERIFICATION.md written with all CRAWL requirements showing PASS
- Phase 1 is complete per ROADMAP.md success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/01-crawl-errors-indexability/01-03-SUMMARY.md`

Also update .planning/ROADMAP.md: mark Phase 1 plans 01-01, 01-02, 01-03 as complete (change `- [ ]` to `- [x]`).
Also update .planning/REQUIREMENTS.md: mark CRAWL-01 through CRAWL-05 as complete (change `- [ ]` to `- [x]`).
</output>
