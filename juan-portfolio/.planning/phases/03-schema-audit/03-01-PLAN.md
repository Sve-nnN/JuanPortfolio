---
phase: 03-schema-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-schema-audit/03-AUDIT-NOTES.md
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05]

must_haves:
  truths:
    - "All schema emission points are identified (which file, which schema type)"
    - "Gaps between current output and required fields are documented"
    - "The audit note is specific enough that 03-02 through 03-04 need no further investigation"
  artifacts:
    - path: ".planning/phases/03-schema-audit/03-AUDIT-NOTES.md"
      provides: "Gap analysis consumed by plans 03-02 through 03-04"
      contains: "BlogPosting gaps, homepage gaps, BreadcrumbList status per page type"
  key_links:
    - from: "src/app/(frontend)/[locale]/[slug]/page.tsx"
      to: "src/components/JsonLd.tsx"
      via: "<JsonLd isHome={true} />"
      pattern: "isHome.*true"
    - from: "src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx"
      to: "src/utilities/generateSchema.ts"
      via: "generateSchema({ doc: post, collection: 'posts', url, breadcrumbs })"
      pattern: "generateSchema"
---

<objective>
Audit the current schema.org implementation and document all gaps in a concise notes file.

Purpose: Plans 03-02 through 03-04 are implementation plans — they need no investigation. This plan produces the ground truth they act on.
Output: `.planning/phases/03-schema-audit/03-AUDIT-NOTES.md`
</objective>

<execution_context>
@/Users/juan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-schema-audit/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace all schema emission points and document gaps</name>
  <files>.planning/phases/03-schema-audit/03-AUDIT-NOTES.md</files>
  <action>
Read the following files (they are the complete picture — no additional exploration needed):

1. `src/utilities/generateSchema.ts` — main schema generator used by post pages and generic pages
2. `src/components/JsonLd.tsx` — renders JSON-LD; handles isHome, post auto-schema, and explicit schema prop
3. `src/app/(frontend)/[locale]/[slug]/page.tsx` — homepage and generic page handler; calls `<JsonLd isHome blocks locale siteUrl />`
4. `src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx` — post page; calls `generateSchema` then passes result as `schema` prop to `<JsonLd schema={schema} post={post} />`
5. `src/app/(frontend)/[locale]/blog/[category]/page.tsx` — category page; calls `generateBreadcrumbSchema` + `generateCollectionPageSchema` + `mergeSchemas`
6. `src/utilities/schema/generateArticleSchema.ts` — standalone article schema builder (check if used anywhere)
7. `src/utilities/schema/generatePersonSchema.ts` — Person schema builder (check if called from homepage)

From this reading, produce `.planning/phases/03-schema-audit/03-AUDIT-NOTES.md` with the following sections:

## Schema Emission Map

Table with columns: Page | Route file | Schema types emitted | How (which function/component)

Include rows for:
- Homepage (`/`)
- Blog post page (`/blog/[category]/[slug]`)
- Blog category page (`/blog/[category]`)
- Blog listing page (`/blog`)
- Generic CMS page

## Gaps Found

### Homepage
- List what is currently emitted
- List what is MISSING per Phase 3 requirements (Person, ProfessionalService)

### BlogPosting (post pages)
- Confirm BlogPosting is emitted from `generateSchema` (not from the JsonLd auto-path, since schema prop is provided)
- Check if `mainEntityOfPage` is present in `generateSchema`'s BlogPosting output — it is NOT currently (no such field in the code)
- Check if `@id` is set on the BlogPosting node — it is NOT (only `url` is inherited from baseSchema spread)
- Note whether `url` field on the article satisfies schema.org's `url` requirement

### BreadcrumbList
- Post pages: confirm breadcrumbs array is built and passed to `generateSchema`, which calls `generateBreadcrumbSchema` and wraps in `@graph`
- Category pages: confirm `generateBreadcrumbSchema` is called directly

### generateArticleSchema.ts
- Note whether this function is called from any active page component (search result: it is NOT — it is exported from the schema index but not used in any page; `generateSchema.ts` implements its own BlogPosting inline)

## Action Items for Each Plan

Brief bullet list per plan (03-02, 03-03) describing exact changes needed.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-schema-audit/03-AUDIT-NOTES.md && grep -q "Schema Emission Map" .planning/phases/03-schema-audit/03-AUDIT-NOTES.md && grep -q "mainEntityOfPage" .planning/phases/03-schema-audit/03-AUDIT-NOTES.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    File exists at `.planning/phases/03-schema-audit/03-AUDIT-NOTES.md` with Schema Emission Map table, Gaps Found section covering homepage and BlogPosting, BreadcrumbList status per page type, and Action Items per downstream plan.
  </done>
</task>

</tasks>

<verification>
Run the automated verify command. The audit notes file must exist, contain the emission map, and call out the mainEntityOfPage gap.
</verification>

<success_criteria>
03-AUDIT-NOTES.md exists and contains enough specificity that an executor of plans 03-02 and 03-03 needs zero additional code investigation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-audit/03-schema-audit-01-SUMMARY.md` with:
- What was found (schema emission map summary)
- Key gaps identified
- Files read during audit
</output>
