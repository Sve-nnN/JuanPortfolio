---
phase: 04-author-profile
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: true
requirements:
  - AUTHOR-02

must_haves:
  truths:
    - "The Juan Carlos Angulo user record in Payload has a non-empty bio field (ES locale)"
    - "The user record has jobTitle populated in ES and EN locales"
    - "The user record has socialMedia.linkedin and socialMedia.github populated"
    - "No new user record is created — the existing record is updated"
  artifacts:
    - path: .planning/phases/04-author-profile/04-02-SUMMARY.md
      provides: "Confirmation of Payload user record state after update"
      min_lines: 10
  key_links:
    - from: "Payload Users collection"
      to: "author page /authors/juan-carlos-angulo"
      via: "slug field on user record"
      pattern: "slug.*juan-carlos-angulo"
---

<objective>
Populate the Juan Carlos Angulo author record in Payload CMS with bio, jobTitle, and social media links.

Purpose: The Payload user record drives the author bio page at /authors/juan-carlos-angulo and feeds generatePersonSchema. Without populated fields the page renders near-empty content, which hurts E-E-A-T signals.

Output: Updated Payload user record with bio (ES), jobTitle (ES + EN), and socialMedia.linkedin + socialMedia.github filled in. Avatar is intentionally skipped — user will upload manually.
</objective>

<execution_context>
@/Users/juan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-author-profile/04-CONTEXT.md
@.planning/phases/04-author-profile/04-01-SUMMARY.md

<interfaces>
From src/collections/Users/index.ts — fields available on the Users collection:

```
name          text, required
role          text (admin-only)
jobTitle      text, localized (ES + EN)
bio           textarea, localized (ES + EN)
expertise     array [ { topic: text } ]
socialMedia   group {
                linkedin: text
                github:   text
                twitter:  text
                website:  text
              }
education     array [ { degree, institution, startDate, endDate, description, logo (media), certificate (media) } ]
experience    array [ { company, role, startDate, endDate, description } ]
avatar        upload (media)
slug          text (auto-generated from name)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find and update the Juan Carlos Angulo user record via Payload MCP</name>
  <files></files>
  <action>
    Use the Payload MCP to update the existing user record. Do NOT create a new record.

    Step 1 — Find the user record:
    Query the `users` collection for a record where name contains "Juan" or slug equals "juan-carlos-angulo". Note the record `id`.

    Step 2 — Update the record with the following data:

    Fields to set (ES locale unless noted):
    - `name`: "Juan Carlos Angulo"
    - `jobTitle` (ES locale): "Ingeniero de Software y Consultor SEO Técnico"
    - `jobTitle` (EN locale): "Software Engineer & Technical SEO Consultant"
    - `bio` (ES locale): A ~150-word professional bio extracted from content/author-profile.md "Sobre mí" section. Use the first two paragraphs, condensed. Must be factual (Lima, Perú; 4 years; Next.js; Payload CMS; SEO técnico).
    - `bio` (EN locale): A ~80-word bio extracted from content/author-profile.md "About Me" section. First paragraph only.
    - `socialMedia.linkedin`: "https://www.linkedin.com/in/juancangulo/"
    - `socialMedia.github`: "https://github.com/sve-nnn"
    - `socialMedia.website`: "https://juan-tech.com"

    Fields to leave untouched:
    - `avatar` — do not set, do not clear
    - `education`, `experience`, `expertise` — do not overwrite if already populated; only set if empty
    - `role`, `email`, `password` — never touch

    Step 3 — Verify the update succeeded:
    Re-query the user by id and confirm bio, jobTitle, and socialMedia fields are non-empty.

    Step 4 — Note the user record `id` in the summary — it will be needed by plan 04-03 for the author bulk-assign.
  </action>
  <verify>
    <automated>MISSING — Payload MCP query: find users where slug=juan-carlos-angulo and confirm bio is non-empty</automated>
  </verify>
  <done>
    - bio (ES) is >= 100 characters
    - jobTitle (ES) = "Ingeniero de Software y Consultor SEO Técnico"
    - socialMedia.linkedin = "https://www.linkedin.com/in/juancangulo/"
    - socialMedia.github = "https://github.com/sve-nnn"
    - No new user record created (totalDocs for users collection unchanged)
  </done>
</task>

</tasks>

<verification>
Via Payload MCP:
1. Query users where slug = "juan-carlos-angulo"
2. Confirm response includes bio (non-empty), jobTitle, socialMedia.linkedin, socialMedia.github
3. Confirm totalDocs for users collection is the same before and after (no extra record created)
</verification>

<success_criteria>
The Payload user record for Juan Carlos Angulo has bio, jobTitle (both locales), and socialMedia (LinkedIn + GitHub) populated. The record id is documented in the summary for use by plan 04-03.
</success_criteria>

<output>
After completion, create .planning/phases/04-author-profile/04-02-SUMMARY.md with:
- User record id
- Final state of bio (ES) — first 100 chars
- Final state of jobTitle ES and EN
- Final state of socialMedia fields
- Confirmation: no new user created
</output>
