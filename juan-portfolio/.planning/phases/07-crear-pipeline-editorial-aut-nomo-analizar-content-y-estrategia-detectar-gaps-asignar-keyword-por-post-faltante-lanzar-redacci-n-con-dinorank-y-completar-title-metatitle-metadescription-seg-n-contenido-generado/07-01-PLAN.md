---
phase: 07-crear-pipeline-editorial-aut-nomo-analizar-content-y-estrategia-detectar-gaps-asignar-keyword-por-post-faltante-lanzar-redacci-n-con-dinorank-y-completar-title-metatitle-metadescription-seg-n-contenido-generado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/engine/types.ts
  - src/scripts/engine/gap-analyzer.ts
  - src/scripts/engine/keyword-assigner.ts
  - tests/unit/scripts/gap-analyzer.test.ts
  - tests/unit/scripts/keyword-assigner.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
must_haves:
  truths:
    - "Pipeline can produce a deterministic missing-post candidate list from content and strategy inputs"
    - "Each candidate receives at most one keyword assignment in one run"
  artifacts:
    - path: "src/scripts/engine/types.ts"
      provides: "Shared contracts for gaps and assignments"
    - path: "src/scripts/engine/gap-analyzer.ts"
      provides: "Gap discovery from markdown sources"
    - path: "src/scripts/engine/keyword-assigner.ts"
      provides: "Deterministic keyword mapping"
  key_links:
    - from: "src/scripts/engine/gap-analyzer.ts"
      to: "content/posts/**/*.md"
      via: "filesystem scan"
      pattern: "glob|existsSync|readFileSync"
    - from: "src/scripts/engine/keyword-assigner.ts"
      to: "content/keywords_backlog.md"
      via: "markdown table parse"
      pattern: "parseKeywordsMarkdown"
---

<objective>
Create the phase-07 core contracts and deterministic analyzers so downstream orchestration can run without exploratory coding.

Purpose: Establish reliable, testable foundations for autonomous editorial decisions.
Output: Typed contracts + gap analyzer + keyword assigner with unit tests.
</objective>

<execution_context>
@/Users/juan/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/juan/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crear-pipeline-editorial-aut-nomo-analizar-content-y-estrategia-detectar-gaps-asignar-keyword-por-post-faltante-lanzar-redacci-n-con-dinorank-y-completar-title-metatitle-metadescription-seg-n-contenido-generado/07-RESEARCH.md
@src/scripts/create-post.ts
@src/scripts/utils/markdownTable.ts

<interfaces>
From src/scripts/utils/markdownTable.ts:
```ts
export function parseKeywordsMarkdown(content: string): { keywords: KeywordData[], headers: string[] }
```

From src/scripts/create-post.ts:
```ts
export interface KeywordData {
  keyword: string
  targetUrl: string
  category: string
  slug: string
  filePath: string
  fileExists: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared pipeline contracts</name>
  <files>src/scripts/engine/types.ts</files>
  <behavior>
    - Export type contracts for gap candidates, keyword assignments, and assignment scoring input.
    - Keep contracts independent from runtime libraries so analyzer and assigner remain pure.
  </behavior>
  <action>Create `src/scripts/engine/types.ts` with `GapCandidate`, `KeywordAssignment`, `GapAnalyzerInput`, and `AssignmentInput` types. Avoid embedding file I/O in contracts. Add explicit locale union (`'es' | 'en'`) and source enums for deterministic reporting.</action>
  <verify>
    <automated>pnpm test:int -- tests/unit/scripts/gap-analyzer.test.ts --runInBand</automated>
  </verify>
  <done>Type file exists, is imported by analyzer/assigner tests, and compiles without `any`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic gap analyzer</name>
  <files>src/scripts/engine/gap-analyzer.ts, tests/unit/scripts/gap-analyzer.test.ts</files>
  <behavior>
    - Test: existing post slug is not returned as gap.
    - Test: strategy entry with missing slug returns one normalized gap candidate.
    - Test: malformed rows are ignored without throwing.
  </behavior>
  <action>Implement analyzer that reads normalized strategy rows and existing post slugs, returns sorted unique `GapCandidate[]` by `(locale, category, slug)`. Add parser guards for missing target URLs and empty slugs. Keep this module pure (input arrays in, output array out).</action>
  <verify>
    <automated>pnpm test:int -- tests/unit/scripts/gap-analyzer.test.ts</automated>
  </verify>
  <done>Analyzer outputs stable, deduplicated gap lists and unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement keyword assigner with de-duplication</name>
  <files>src/scripts/engine/keyword-assigner.ts, tests/unit/scripts/keyword-assigner.test.ts</files>
  <behavior>
    - Test: each gap receives at most one keyword.
    - Test: already-consumed keyword is not reused in same run.
    - Test: tie-breaking is deterministic (score then lexical slug).
  </behavior>
  <action>Implement assigner that scores candidate keywords (volume up, difficulty down), filters incompatible locale/category pairs, and produces deterministic `KeywordAssignment[]`. Persist no I/O here; return pure data structures only.</action>
  <verify>
    <automated>pnpm test:int -- tests/unit/scripts/keyword-assigner.test.ts</automated>
  </verify>
  <done>Assignments are deterministic, deduplicated, and unit-tested.</done>
</task>

</tasks>

<verification>
Run both targeted unit suites:
- `pnpm test:int -- tests/unit/scripts/gap-analyzer.test.ts`
- `pnpm test:int -- tests/unit/scripts/keyword-assigner.test.ts`
</verification>

<success_criteria>
- Deterministic gap and assignment modules exist and are tested.
- Downstream orchestration can consume typed contracts without schema guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/07-crear-pipeline-editorial-aut-nomo-analizar-content-y-estrategia-detectar-gaps-asignar-keyword-por-post-faltante-lanzar-redacci-n-con-dinorank-y-completar-title-metatitle-metadescription-seg-n-contenido-generado/07-01-SUMMARY.md`
</output>
