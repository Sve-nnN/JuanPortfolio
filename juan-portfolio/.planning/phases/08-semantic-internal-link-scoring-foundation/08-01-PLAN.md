---
phase: 08-semantic-internal-link-scoring-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/internal-linking/semantic/types.ts
  - src/scripts/internal-linking/semantic/embedding-provider.ts
  - src/scripts/internal-linking/types.ts
  - tests/unit/internal-linking-semantic-types.test.ts
autonomous: true
requirements:
  - LINK-01
must_haves:
  truths:
    - "Semantic scoring has explicit contracts that represent vector similarity and weighted totals"
    - "The engine can run with or without a live embedding provider"
  artifacts:
    - path: "src/scripts/internal-linking/semantic/types.ts"
      provides: "Semantic scoring and provider interfaces"
    - path: "src/scripts/internal-linking/semantic/embedding-provider.ts"
      provides: "Provider abstraction and fallback behavior"
    - path: "src/scripts/internal-linking/types.ts"
      provides: "Extended link opportunity shape for scored outputs"
  key_links:
    - from: "src/scripts/internal-linking/semantic/embedding-provider.ts"
      to: "src/scripts/internal-linking/semantic/types.ts"
      via: "interface implementation"
      pattern: "implements|EmbeddingProvider"
    - from: "src/scripts/internal-linking/types.ts"
      to: "src/scripts/internal-linking/ContentScanner.ts"
      via: "opportunity typing"
      pattern: "LinkOpportunity|relevance|semantic"
---

<objective>
Define semantic-scoring contracts and provider abstraction before changing scanner behavior.

Purpose: prevent downstream implementation ambiguity and keep testability high.
Output: typed interfaces + fallback-capable provider layer + contract tests.
</objective>

<execution_context>
@/Users/juan/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/juan/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-semantic-internal-link-scoring-foundation/08-CONTEXT.md
@.planning/phases/08-semantic-internal-link-scoring-foundation/08-RESEARCH.md
@src/scripts/internal-linking/types.ts
@src/scripts/internal-linking/ContentScanner.ts

<interfaces>
From src/scripts/internal-linking/types.ts:
```ts
export interface LinkOpportunity {
  sourcePost: PostMetadata
  targetPost: PostMetadata
  keyword: string
  context: string
  lineNumber: number
  relevance: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create semantic scoring contracts</name>
  <files>src/scripts/internal-linking/semantic/types.ts, src/scripts/internal-linking/types.ts</files>
  <behavior>
    - Test: semantic score model includes raw vector score and weighted total score.
    - Test: scored opportunity can extend existing LinkOpportunity without breaking current fields.
  </behavior>
  <action>Create semantic contracts (`SemanticScore`, `ScoredOpportunity`, `ScoringWeights`, `EmbeddingProvider`) and extend the existing opportunity typing to optionally include semantic metadata. Keep backward compatibility with existing scanner and injector code paths.</action>
  <verify>
    <automated>pnpm test:int -- tests/unit/internal-linking-semantic-types.test.ts</automated>
  </verify>
  <done>Type contracts compile and preserve compatibility with existing opportunity consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement embedding provider abstraction with deterministic fallback</name>
  <files>src/scripts/internal-linking/semantic/embedding-provider.ts, tests/unit/internal-linking-semantic-types.test.ts</files>
  <behavior>
    - Test: provider returns deterministic fallback vectors when external provider is unavailable.
    - Test: provider interface allows async embedding generation for source and target text.
  </behavior>
  <action>Implement provider abstraction with a default fallback implementation suitable for tests and local runs. Do not perform hard network dependency in constructor. Expose a factory that can later wire OpenAI/Gemini based on env flags.</action>
  <verify>
    <automated>pnpm test:int -- tests/unit/internal-linking-semantic-types.test.ts</automated>
  </verify>
  <done>Fallback provider behaves deterministically and can be consumed by scorer implementation.</done>
</task>

</tasks>

<verification>
- `pnpm test:int -- tests/unit/internal-linking-semantic-types.test.ts`
</verification>

<success_criteria>
- Semantic contracts exist and are reusable by scanner/admin layers.
- Embedding provider abstraction is testable and deterministic without external API calls.
</success_criteria>

<output>
After completion, create `.planning/phases/08-semantic-internal-link-scoring-foundation/08-01-SUMMARY.md`
</output>
