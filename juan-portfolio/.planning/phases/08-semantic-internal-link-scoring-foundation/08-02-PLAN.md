---
phase: 08-semantic-internal-link-scoring-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/scripts/internal-linking/semantic/scorer.ts
  - src/scripts/internal-linking/ContentScanner.ts
  - src/scripts/internal-linking/types.ts
  - tests/int/internal-linking-semantic-scoring.test.ts
autonomous: true
requirements:
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - "Scanner ranking uses semantic vector similarity together with existing lexical and cluster signals"
    - "Anchor candidates remain safe for markdown replacement and keep previous exclusion behavior"
  artifacts:
    - path: "src/scripts/internal-linking/semantic/scorer.ts"
      provides: "Weighted semantic ranking implementation"
    - path: "src/scripts/internal-linking/ContentScanner.ts"
      provides: "Integrated semantic scoring pipeline"
    - path: "tests/int/internal-linking-semantic-scoring.test.ts"
      provides: "Integration proof for scored opportunities"
  key_links:
    - from: "src/scripts/internal-linking/ContentScanner.ts"
      to: "src/scripts/internal-linking/semantic/scorer.ts"
      via: "score computation"
      pattern: "score|semantic|weights"
    - from: "src/scripts/internal-linking/ContentScanner.ts"
      to: "src/scripts/internal-linking/LinkInjector.ts"
      via: "LinkOpportunity compatibility"
      pattern: "LinkOpportunity"
---

<objective>
Implement weighted semantic scoring and integrate it into scanner output without regressing safe replacement behavior.

Purpose: make link opportunities semantically ranked and ready for reliable application.
Output: scorer module + scanner integration + integration tests.
</objective>

<execution_context>
@/Users/juan/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/juan/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-semantic-internal-link-scoring-foundation/08-01-PLAN.md
@src/scripts/internal-linking/ContentScanner.ts
@src/scripts/internal-linking/LinkInjector.ts
@src/scripts/internal-linking/semantic/types.ts

<interfaces>
From src/scripts/internal-linking/ContentScanner.ts:
```ts
scanPost(post: PostMetadata, allPosts: PostMetadata[]): LinkOpportunity[]
```

From src/scripts/internal-linking/LinkInjector.ts:
```ts
applyLinks(opportunities: Map<string, LinkOpportunity[]>, dryRun: boolean): LinkingResult
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build weighted semantic scorer</name>
  <files>src/scripts/internal-linking/semantic/scorer.ts, tests/int/internal-linking-semantic-scoring.test.ts</files>
  <behavior>
    - Test: higher semantic similarity increases final score when lexical scores are tied.
    - Test: deterministic tie-breakers return stable ordering across runs.
  </behavior>
  <action>Create scorer functions that combine lexical score, cluster relevance, and semantic vector score with explicit weights. Add deterministic tie-breakers (`totalScore desc`, `keyword length desc`, `target slug asc`). Keep functions pure and side-effect free.</action>
  <verify>
    <automated>pnpm test:int -- tests/int/internal-linking-semantic-scoring.test.ts</automated>
  </verify>
  <done>Scorer yields stable, weighted rankings and integration tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate scorer into ContentScanner output</name>
  <files>src/scripts/internal-linking/ContentScanner.ts, src/scripts/internal-linking/types.ts, tests/int/internal-linking-semantic-scoring.test.ts</files>
  <behavior>
    - Test: scanAllPosts returns semantically enriched opportunities preserving existing fields.
    - Test: exclusion rules (headings, code, link contexts) still block unsafe anchors.
  </behavior>
  <action>Update scanner to call semantic scorer before returning opportunities. Preserve current filtering pipeline (self-link prevention, locale isolation, category checks, duplicate prevention). Ensure resulting opportunities are still consumable by `LinkInjector` unchanged.</action>
  <verify>
    <automated>pnpm test:int -- tests/int/internal-linking-semantic-scoring.test.ts -- tests/int/internal-linking-exclusions.test.ts</automated>
  </verify>
  <done>Semantic ranking is active, exclusions remain intact, and injector compatibility is preserved.</done>
</task>

</tasks>

<verification>
- `pnpm test:int -- tests/int/internal-linking-semantic-scoring.test.ts`
- `pnpm test:int -- tests/int/internal-linking-exclusions.test.ts`
</verification>

<success_criteria>
- Semantic scoring is integrated into scanner ranking with deterministic behavior.
- Safe anchor selection and existing exclusions still hold.
</success_criteria>

<output>
After completion, create `.planning/phases/08-semantic-internal-link-scoring-foundation/08-02-SUMMARY.md`
</output>
