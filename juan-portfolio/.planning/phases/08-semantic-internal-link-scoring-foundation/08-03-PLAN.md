---
phase: 08-semantic-internal-link-scoring-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/scripts/build-internal-links.ts
  - src/scripts/services/LinkService.ts
  - tests/int/internal-linking.test.ts
  - .planning/phases/08-semantic-internal-link-scoring-foundation/08-VERIFICATION.md
autonomous: true
requirements:
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - "CLI and service flows run semantic scoring by default in dry-run and apply modes"
    - "Phase verification proves semantic ranking and safe anchor replacement behavior"
  artifacts:
    - path: "src/scripts/build-internal-links.ts"
      provides: "Semantic scoring execution path in CLI"
    - path: "src/scripts/services/LinkService.ts"
      provides: "Service-level semantic linking integration"
    - path: ".planning/phases/08-semantic-internal-link-scoring-foundation/08-VERIFICATION.md"
      provides: "Phase evidence and pass/fail checks"
  key_links:
    - from: "src/scripts/build-internal-links.ts"
      to: "src/scripts/internal-linking/ContentScanner.ts"
      via: "scanner instantiation"
      pattern: "new ContentScanner"
    - from: "src/scripts/services/LinkService.ts"
      to: "src/scripts/internal-linking/ContentScanner.ts"
      via: "service orchestration"
      pattern: "scanAllPosts"
---

<objective>
Wire semantic scoring through the executable entrypoints and close the phase with verifiable evidence.

Purpose: ensure production flows consume the new foundation consistently.
Output: CLI/service integration updates + final verification report.
</objective>

<execution_context>
@/Users/juan/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/juan/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-semantic-internal-link-scoring-foundation/08-02-PLAN.md
@src/scripts/build-internal-links.ts
@src/scripts/services/LinkService.ts
@tests/int/internal-linking.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire semantic scoring in CLI and service entrypoints</name>
  <files>src/scripts/build-internal-links.ts, src/scripts/services/LinkService.ts, tests/int/internal-linking.test.ts</files>
  <behavior>
    - Test: CLI dry-run path uses semantically scored opportunities.
    - Test: service `buildLinks` path produces semantically ordered opportunities before injection.
  </behavior>
  <action>Update CLI and `LinkService` configuration paths so scanner uses semantic scorer defaults from Phase 08. Keep backward-compatible flags and output. Do not change locale-isolation logic or dry-run semantics.</action>
  <verify>
    <automated>pnpm test:int -- tests/int/internal-linking.test.ts</automated>
  </verify>
  <done>Both entrypoints execute semantic ranking consistently and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Produce phase verification report</name>
  <files>.planning/phases/08-semantic-internal-link-scoring-foundation/08-VERIFICATION.md</files>
  <action>Run targeted internal-linking tests, record command outputs, and evaluate each phase success criterion as PASS/FAIL with concrete evidence. Include any residual risk for Phase 09 consumption.</action>
  <verify>
    <automated>pnpm test:int -- tests/unit/internal-linking.test.ts -- tests/int/internal-linking.test.ts -- tests/int/internal-linking-exclusions.test.ts</automated>
  </verify>
  <done>Verification report exists with explicit criterion mapping and test evidence.</done>
</task>

</tasks>

<verification>
- `pnpm test:int -- tests/int/internal-linking.test.ts`
- `pnpm test:int -- tests/int/internal-linking-exclusions.test.ts`
- `pnpm test:int -- tests/unit/internal-linking.test.ts`
</verification>

<success_criteria>
- Semantic scoring is active in all executable linking flows.
- Verification evidence is documented and ready for Phase 09 planning dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/08-semantic-internal-link-scoring-foundation/08-03-SUMMARY.md`
</output>
