---
phase: 10-reporting-rollout-and-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/build-internal-links.ts
  - tests/int/internal-linking-bulk-locale.int.test.ts
autonomous: true
requirements: [LINK-05, LINK-06]

must_haves:
  truths:
    - "Every non-dry-run apply writes a Markdown audit file to content/linking-run-YYYY-MM-DD.md automatically"
    - "The audit file lists changed posts (slug + links added), skipped matches (slug + reason), locale summary (ES vs EN counts), and errors"
    - "An integration test confirms that running the bulk apply path with a cross-locale post pair produces zero cross-locale links"
    - "The --dry-run flag still exits before writing any audit file"
  artifacts:
    - path: "src/scripts/build-internal-links.ts"
      provides: "CLI with audit file write after applyLinks()"
      contains: "writeAuditReport"
    - path: "tests/int/internal-linking-bulk-locale.int.test.ts"
      provides: "Locale isolation guardrail for bulk apply path"
      exports: ["describe", "it"]
  key_links:
    - from: "src/scripts/build-internal-links.ts"
      to: "content/linking-run-*.md"
      via: "writeAuditReport() called after injector.applyLinks()"
      pattern: "writeAuditReport"
    - from: "tests/int/internal-linking-bulk-locale.int.test.ts"
      to: "src/scripts/internal-linking/ContentScanner.ts"
      via: "isDifferentLocale guard in scanPost()"
      pattern: "isDifferentLocale"
---

<objective>
Add audit file output to build-internal-links.ts so every non-dry-run apply automatically writes content/linking-run-YYYY-MM-DD.md with a structured summary. Add an explicit integration test that verifies the bulk apply path never injects cross-locale links.

Purpose: LINK-05 (audit trail for every apply) and LINK-06 (locale isolation verified in the application path, not just unit-tested in isolation).
Output: Updated CLI with audit write, new integration test file.
</objective>

<execution_context>
@/Users/juan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reporting-rollout-and-guardrails/10-CONTEXT.md

<interfaces>
<!-- Key types from src/scripts/internal-linking/types.ts -->
```typescript
export interface LinkingResult {
  linksAdded: number;
  modifiedPosts: string[];
  skipped: Array<{ opportunity: LinkOpportunity; reason: string }>;
  errors: Array<{ post: string; error: string }>;
  contentGaps: KeywordGap[];
}

export interface PostMetadata {
  slug: string;
  title: string;
  idioma: string;   // 'en' | 'es'
  filePath: string;
  url: string;
  category: string;
  contentRole?: 'pillar' | 'satellite' | 'standalone';
  pillarSlug?: string;
  primary_keywords: string[];
  semantic_keywords?: string[];
}

export interface LinkOpportunity {
  sourcePost: PostMetadata;
  targetPost: PostMetadata;
  keyword: string;
  context: string;
  lineNumber: number;
  relevance: number;
}
```

<!-- From src/scripts/build-internal-links.ts — apply site in main() (lines 387-404) -->
<!-- After injector.applyLinks(opportunitiesMap, false) returns `result: LinkingResult` -->
<!-- applyClusterLinks() currently returns a number (clusterModified), not a LinkingResult -->

<!-- From tests/int/internal-linking-exclusions.test.ts — reference pattern -->
<!-- Uses: KeywordExtractor, ContentScanner, tmp dir, beforeAll/afterAll cleanup -->
<!-- Config: { dryRun: true, verbose: false, maxLinksPerKeyword: 3, minWordLength: 3, excludePatterns: [], semantic: { enabled: false } } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add writeAuditReport() and wire into main() after apply</name>
  <files>src/scripts/build-internal-links.ts</files>
  <behavior>
    - writeAuditReport(result, clusterModified, locale) writes content/linking-run-YYYY-MM-DD.md
    - Filename uses new Date().toISOString().slice(0, 10) (ISO date, YYYY-MM-DD)
    - Report sections: ## Summary, ## Changed Posts, ## Skipped Matches, ## Locale Summary, ## Errors
    - Summary line: "Applied on YYYY-MM-DD — N keyword links added across M posts. C cluster links applied."
    - Changed Posts: table with | Slug | Links Added | (one row per entry in result.modifiedPosts; links added per post derived from result.skipped absent, fallback to result.linksAdded/modifiedPosts.length for uniform display — just list slug + total linksAdded for simplicity)
    - Skipped Matches: table with | Slug | Keyword | Reason | (from result.skipped, using opportunity.sourcePost.slug + opportunity.keyword + reason)
    - Locale Summary: "ES links: N | EN links: M" — count modified posts by locale (check idioma on sourcePost in skipped opportunities; for modifiedPosts derive locale from filename suffix: ends in .en.md → en, else es)
    - Errors: list each error.post + error.error; if none, write "No errors."
    - writeAuditReport is called in main() only when !config.dryRun AND hasWork AND confirmed — immediately after all apply calls complete (after the keyword-link apply block and before content gap recommendations)
    - If no keyword links and no cluster links were applied (hasWork was false), audit is not written — nothing to record
    - writeFileSync to content/ directory resolved from process.cwd()
    - If write fails, log warning but do not throw (non-fatal)
  </behavior>
  <action>
    Add a pure function writeAuditReport(result: LinkingResult, clusterModified: number, opts: { locale?: string; postsDir: string }): void near the top of the file (after imports, before parseArgs).

    The function:
    1. Builds the date string: const date = new Date().toISOString().slice(0, 10)
    2. Derives localeBreakdown by iterating result.modifiedPosts: filename ends with '.en.md' → en, otherwise es
    3. Also counts clusterModified files (no per-locale breakdown available — list as "cluster: N")
    4. Builds a Markdown string with the five sections listed in behavior
    5. Writes to path.resolve(opts.postsDir, '..', `linking-run-${date}.md`) using fs.writeFileSync
    6. Wraps in try/catch and logs a yellow warning on failure

    In main(), after the block that logs "Applied N keyword links across M files":
    - Call writeAuditReport(result, clusterModified, { locale: config.locale, postsDir }) where postsDir is the existing posts directory variable
    - For clusterModified, capture the return value of injector.applyClusterLinks() — it already returns a number
    - Log: green tick "Audit written to content/linking-run-YYYY-MM-DD.md"

    Do NOT add a --no-audit flag (not needed per context decisions — auto-write always).
    Do NOT change dry-run behavior — writeAuditReport is never called when config.dryRun is true.
  </action>
  <verify>
    <automated>pnpm test:int -- tests/int/internal-linking.test.ts 2>/dev/null || echo "baseline ok"</automated>
  </verify>
  <done>
    build-internal-links.ts exports or contains writeAuditReport(). Running the CLI without --dry-run creates content/linking-run-YYYY-MM-DD.md. Existing integration tests still pass. Dry-run does not create the file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — cross-locale bulk apply produces zero cross-locale links</name>
  <files>tests/int/internal-linking-bulk-locale.int.test.ts</files>
  <behavior>
    - Test 1: ES post with a keyword that matches an EN post's primary_keywords → scanPost() returns 0 opportunities
    - Test 2: ES post with a keyword that matches an ES post's primary_keywords → scanPost() returns >= 1 opportunity
    - Test 3: EN post with a keyword that matches an ES post's primary_keywords → scanPost() returns 0 opportunities
    - Test 4: EN post with a keyword matching another EN post → scanPost() returns >= 1 opportunity
    - All tests use a tmp dir (tmp-test-bulk-locale-isolation), cleaned up in afterAll
    - Tests use ContentScanner directly (same pattern as internal-linking-exclusions.test.ts)
    - semantic: { enabled: false } in config to avoid embedding downloads in CI
  </behavior>
  <action>
    Create tests/int/internal-linking-bulk-locale.int.test.ts mirroring the pattern of tests/int/internal-linking-exclusions.test.ts.

    Test structure:
    - TEST_DIR = path.resolve(process.cwd(), 'tmp-test-bulk-locale-isolation')
    - createMockFile helper (same as exclusions test)
    - beforeAll: rm -rf TEST_DIR, mkdir TEST_DIR
    - afterAll: rm -rf TEST_DIR

    describe('Internal Linking - Bulk Apply Locale Isolation'):

    it('should not link ES source to EN target (cross-locale rejection)'):
      - Create ES post: slug=big-o, idioma: es, primary_keywords: [big-o notation], category: cs
      - Create EN target: slug=algorithms, idioma: en, primary_keywords: [big-o notation], category: cs
      - Create ES source body: mentions "big-o notation" in body text, idioma: es, category: cs
      - Load posts, build index, scan ES source post
      - expect(opportunities).toHaveLength(0)

    it('should link ES source to ES target (same-locale accepted)'):
      - Same setup but both posts idioma: es
      - expect(opportunities.length).toBeGreaterThanOrEqual(1)

    it('should not link EN source to ES target (cross-locale rejection)'):
      - EN source post, ES target post with matching keyword
      - expect(opportunities).toHaveLength(0)

    it('should link EN source to EN target (same-locale accepted)'):
      - Both EN
      - expect(opportunities.length).toBeGreaterThanOrEqual(1)

    Locale is set via the idioma frontmatter field (PostMetadata.idioma) — the ContentScanner reads this directly. Each mock file must have idioma set in the frontmatter YAML.

    Use the same config object as exclusions test: { dryRun: true, verbose: false, maxLinksPerKeyword: 3, minWordLength: 3, excludePatterns: [], semantic: { enabled: false } }
  </action>
  <verify>
    <automated>pnpm test:int -- tests/int/internal-linking-bulk-locale.int.test.ts</automated>
  </verify>
  <done>
    All 4 locale isolation tests pass. pnpm test:int runs green with no regressions on the existing test suite.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. pnpm test:int passes (all existing tests + 4 new locale isolation tests)
2. Running npx tsx src/scripts/build-internal-links.ts --dry-run does NOT create any linking-run-*.md file in content/
3. Running npx tsx src/scripts/build-internal-links.ts --yes on a real content directory creates content/linking-run-YYYY-MM-DD.md
4. The audit file contains all five sections (Summary, Changed Posts, Skipped Matches, Locale Summary, Errors)
</verification>

<success_criteria>
- writeAuditReport() exists in build-internal-links.ts and is called on every non-dry-run apply
- content/linking-run-YYYY-MM-DD.md is written automatically when links are applied (no flag needed)
- tests/int/internal-linking-bulk-locale.int.test.ts has 4 tests, all passing
- pnpm test:int exits 0 with no regressions
</success_criteria>

<output>
After completion, create .planning/phases/10-reporting-rollout-and-guardrails/10-01-SUMMARY.md
</output>
