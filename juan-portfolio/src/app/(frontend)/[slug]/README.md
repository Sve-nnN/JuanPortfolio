# Dynamic Page Route

This folder contains the catch-all route for dynamic pages. It renders pages from the Payload CMS based on the slug.

## Files

- **page.tsx:** The server-side component that fetches the page data from the CMS.
- **page.client.tsx:** The client-side component that renders the page.
